# Sweep 00 — measurement sweep

**Run:** 2026-09-20T08:25:17Z · `tools/proof-harness` @ `b587127` · `git_dirty: false`
**Handoff:** `docs/handoff/2026-09-20-HANDOFF-00-measurement-sweep.md`
**Rule:** PASS / FAIL / **UNKNOWN**. "Could not check" is UNKNOWN, never PASS.
**No code was changed.** Section A9 temporarily reverse-applied four source hunks
and restored each one; `git status` was verified clean after every step and
`HEAD` is unchanged at `b587127`.

---

## Headline — the five things that change the plan

1. **The "10 of 10 devx tables are unmanaged by CloudFormation" fact is false.**
   All 11 devx tables *are* CloudFormation-managed. They synthesize as
   `AWS::DynamoDB::GlobalTable` (CDK `TableV2`), and `preflight.py:366` looks
   for `AWS::DynamoDB::Table`. The single FAIL in "7 PASS / 1 UNKNOWN / 1 FAIL"
   is a one-line false positive in the harness's own instrument. See **A11**.

2. **The P-28 replacement gate cannot protect any table in this project.**
   `changeset_replacement_report.py` protects `AWS::DynamoDB::Table`; every
   table here is `AWS::DynamoDB::GlobalTable`. The auto-fail that handoff 03's
   step 4 depends on will never fire. See **A8**.

3. **`cdk diff CareerVpCrudDevx` is clean and safe.** Zero table / bucket /
   user-pool / RestApi changes. 31 Lambda code updates, 19 version-alias
   rotations, one IAM statement (the K9 fix), one new `DeployedGitSha` output.
   Handoff 03's stated blocker does not exist. See **B11**.

4. **All four committed fixes have tests that genuinely fail without the fix.**
   4 of 4 RED. This is the good news the session was braced against. See **A9**.

5. **Log retention is 1 day on 31 of 32 devx Lambdas**, not just gap-api. A
   post-deploy incident is undiagnosable the next morning. See **C7**.

Two more worth carrying: **the purchase path is mock end to end** (D4/D6), and
**running the mandatory checks dirties the tree**, which is why proofs keep
coming out `git_dirty: true` (see "Incidental findings").

---

## Section A — inherited claims

| # | Claim | Result | Command and literal output |
|---|---|---|---|
| A1 | env `deploy-dev` exists, **ymeirovich** required reviewer | **PASS** | `gh api repos/:owner/:repo/environments/deploy-dev` → `"name":"deploy-dev"`, `"protection_rules":[{"type":"required_reviewers",...,"reviewers":[{"type":"User","reviewer":{"login":"ymeirovich"}}]},{"type":"branch_policy"}]`, created `2026-09-14T09:57:29Z` |
| A2 | env `staging` exists with required reviewer | **PASS** | same API → `"name":"staging"`, `required_reviewers` → `ymeirovich`, `branch_policy`, created `2026-01-25T08:12:08Z` |
| A3 | both branch-restricted as claimed | **PASS** | both: `"deployment_branch_policy":{"protected_branches":false,"custom_branch_policies":true}`. `.../deploy-dev/deployment-branch-policies` → 1 policy: `main`. `.../staging/...` → 2 policies: `develop`, `main` |
| A4 | IAM user `careervp-review-readonly` exists | **PASS** | `aws iam get-user --user-name careervp-review-readonly` → `"Arn":"arn:aws:iam::788159322332:user/careervp-review-readonly"`, created `2026-09-14T09:59:00Z`, tags `Purpose=P-28-audit-readonly` |
| A5 | policy `CareerVpReviewReadOnly` is genuinely read-only | **PASS** (2 notes) | `list-attached-user-policies` → 1 policy, no inline policies. `get-policy-version v1` → single Allow of 46 actions, all `Describe*`/`Get*`/`List*` plus `dynamodb:Scan`/`Query`, `apigateway:GET`, `logs:FilterLogEvents`/`StartQuery`, `sts:GetCallerIdentity`. **No Put/Update/Delete/Create/Modify verb anywhere.** |
| A6 | harness account exists in pool `us-east-1_bAZ6jb6HP` | **PASS** | `aws cognito-idp admin-get-user` → `"Username":"8428d4e8-d071-7088-a9c3-9e630806436b"`, `email=harness-d2892d12d4@careervp.com`, `"Enabled":true`, `"UserStatus":"CONFIRMED"` |
| A7 | that account has an **active** subscription | **PASS** | `aws dynamodb get-item` on `careervp-users-table-devx` → `{"status":{"S":"active"},"plan":{"S":"monthly"},"updated_at":{"S":"2026-09-14T08:03:34.591505+00:00"}}` |
| A8 | `changeset_replacement_report.py` flags protected types | **FAIL** | `PROTECTED_TYPES = {"AWS::ApiGateway::RestApi","AWS::DynamoDB::Table","AWS::S3::Bucket","AWS::Cognito::UserPool"}`. The stack contains **11 × `AWS::DynamoDB::GlobalTable` and 0 × `AWS::DynamoDB::Table`.** The DynamoDB entry never matches. Logic is otherwise correct (reads CFN's own `Replacement` field, exits 1 on a protected replacement). |
| A9 | the four fixes have tests that fail without the fix | **PASS — 4 of 4** | full detail below |
| A10 | P-05 suite attacks as a real authenticated tenant | **PASS** (1 note) | detail below |

### A5 notes

- The Sid is `DenyAllMutatingByDefault` but the statement is an **Allow**; it
  relies on IAM's implicit deny. Correct, misleadingly named.
- Read-only is not low-impact: `s3:Get*` + `dynamodb:Scan` on `Resource: "*"`
  lets this credential read every customer artifact in the account. One active
  access key exists (`AKIA3PAPU3DOABACKYNW`, created 2026-09-14).

### A9 — the important one. Four of four are genuinely RED without the fix.

Method: `git show <commit> -- <source file> | git apply -R`, run the test,
restore with `git apply`. Test files untouched. `git status` verified clean
between every step.

| Fix | Commit | Source reverted | Test | Result without the fix |
|---|---|---|---|---|
| S0a — VPR export cross-tenant | `10de867` | `handlers/export_handler.py` | `tests/unit/test_export_handler.py` | **RED** — `1 failed, 30 passed`. `test_vpr_export_denies_cross_tenant_access` |
| S0b — VPR status S3 fallback | `ebe6af7` | `handlers/vpr_status_handler.py` | `tests/integration/test_s0b_vpr_status_s3_fallback_ownership.py` | **RED** — `1 failed, 2 passed`. `assert 200 == 404` on `test_job_row_absent_denies_attacker_via_s3_fallback` |
| K9 — artifact-cleanup jobs access | `f8b5321` | `infra/careervp/api_construct.py` | `tests/infrastructure/test_k9_artifact_cleanup_env.py` | **RED** — `2 failed`. Missing `DYNAMODB_TABLE_NAME`; no IAM statement referencing the jobs table |
| quota — stale trial check | `ce850d5` | `handlers/job_handler.py` | `tests/unit/test_job_handler.py` | **RED** — `1 failed, 28 passed`. `assert 403 == 201` on `test_active_subscription_bypasses_exhausted_trial_on_create_job` |

Each failure message names the defect it guards. S0b, K9 and quota fail on clean
assertions against the exact attack or symptom. S0a fails via a
`TypeError: Object of type MagicMock is not JSON serializable` on the unscoped
read path rather than a clean assertion — it is a real failure and correctly
red, but the message does not explain itself the way the other three do.

**Consequence: handoff 03 may proceed on all four fixes.** The commit messages
are backed by evidence.

### A10 — P-05 suite

`src/backend/tests/integration/test_p05_cross_tenant_idor.py`, 177 lines,
12 cases in `p05_owner_check_registry.py`, 3 parametrized tests each (36 total).

- **Owner-positive control present for every case** —
  `test_p05_cross_tenant_owner_positive_control` is parametrized over the same
  `CASES` and asserts `status == 200` for the rightful owner.
- **Attacker is a distinct authenticated identity** —
  `seeding.authed_event` sets `requestContext.authorizer.claims.sub = ATTACKER`.
  Both tenants are really seeded.
- **Note, not a defect:** this is a post-authorizer event under moto, not a live
  Cognito login. That is the correct unit for a per-route ownership check (the
  authorizer is what populates those claims in production), but it is not
  evidence about the authorizer itself.
- **Worth knowing:** 2 of 12 cases (`gap.questions.get`, `company-research.get`)
  list `denial_statuses=(200, 403, 404)` — a denial test that accepts 200. The
  registry documents why (those handlers partition by the *caller's* user_id, so
  a cross-tenant request queries its own empty partition), and the
  `leaked_markers` check still guards the body. Explicit and auditable, but it
  means 2 of 12 denials rest on the marker check alone.

### A11 — new: the unmanaged-tables fact is a false positive (not on the checklist)

```
preflight.py:62-63  STATEFUL_TYPES = ('AWS::DynamoDB::Table', 'AWS::DynamoDB::GlobalTable', ...)
preflight.py:366    managed = {r['physical_id'] for r in inventory.of_type('AWS::DynamoDB::Table')}
```

The constant knows about both types. Line 366 queries only one. Observed:

```
$ aws cloudformation list-stack-resources (root + 5 nested) | grep DynamoDB
AWS::DynamoDB::GlobalTable	careervp-applications-table-devx
AWS::DynamoDB::GlobalTable	careervp-artifacts-table-devx
AWS::DynamoDB::GlobalTable	careervp-company-research-cache-table-devx
AWS::DynamoDB::GlobalTable	careervp-cvs-table-devx
AWS::DynamoDB::GlobalTable	careervp-gap-responses-table-devx
AWS::DynamoDB::GlobalTable	careervp-knowledge-table-devx
AWS::DynamoDB::GlobalTable	careervp-idempotency-table-devx
AWS::DynamoDB::GlobalTable	careervp-identity-map-table-devx
AWS::DynamoDB::GlobalTable	careervp-jobs-table-devx
AWS::DynamoDB::GlobalTable	careervp-users-table-devx
AWS::DynamoDB::GlobalTable	careervp-llm-cache-devx
$ grep -c '"Type": "AWS::DynamoDB::GlobalTable"' infra/cdk.out/CareerVpCrudDevx.template.json
11
```

`review_change.py:52-54` has **both** types and is correct. Only `preflight.py`
and `changeset_replacement_report.py` carry the blind spot.

**This retires a load-bearing premise in the program plan** (W2.3, the B11
warning, and the "10 of 10" row in the Position table). Tables are managed,
retained (`stateful resources retained 18/18 Retain`), and deletion-protected.

---

## Section B — everything that had never been run

| # | Measurement | Result | Output |
|---|---|---|---|
| B1 | backend suite, combined | **FAIL (as predicted)** | `606 failed, 1100 passed, 54 skipped, 4 xfailed, 53 warnings, 100 errors in 144.61s` |
| B2 | backend suite, per directory | **FAIL — 14 real** | table below |
| B3 | frontend jest | **PASS** | `Test Suites: 85 passed, 85 total` · `Tests: 26 skipped, 429 passed, 455 total` · `Ran all test suites in 8 projects` |
| B4 | frontend vitest | **PASS** | `Test Files 67 passed (67)` · `Tests 739 passed (739)` · `Duration 13.01s` |
| B5 | backend `mypy --strict` | **PASS** | `Success: no issues found in 137 source files` |
| B6 | backend `ruff check` | **PASS** | `All checks passed!` |
| B7 | frontend typecheck | **PASS** | `tsc --noEmit` — no output, exit 0 |
| B8 | `cdk synth` | **PASS** | `Successfully synthesized to .../infra/cdk.out`; 1 COG2 MFA warning; `INFO Number of resources: 493 is approaching allowed maximum of 600` |
| B9 | naming validation | **PASS, command is wrong** | `python src/backend/scripts/validate_naming.py --path infra --strict` → `command not found: python` (**exit 127**). With `python3`: exit 0, no output |
| B10 | preflight | **7 PASS / 1 UNKNOWN / 1 FAIL (as predicted)** | full output below |
| B11 | `cdk diff CareerVpCrudDevx` | **PASS — no stateful change** | full analysis below |

### B2 — per directory

```
tests/unit               1430 passed, 15 skipped, 4 xfailed, 118 warnings in 84.28s
tests/integration        11 failed, 185 passed, 17 skipped, 30 warnings in 26.40s
tests/infrastructure     1 failed, 81 passed, 1 skipped, 1 warning in 94.85s
tests/e2e                4 passed, 21 skipped, 4 warnings in 0.44s
tests/models             18 passed in 0.01s
tests/regression         2 failed, 37 passed, 5 warnings in 0.24s
tests/security           3 passed in 0.01s
tests/infra              34 passed in 0.31s
```

**~1792 passed, 14 failed.** Reproduces the recorded baseline exactly, in all
eight directories. The contamination story holds: 606 combined → 14 isolated.

### B5–B9 — the highest-value unknowns, and they are all clean

These had no prediction because nobody had run them. **`mypy --strict` passes on
137 files, `ruff` passes, `tsc --noEmit` passes, `cdk synth` succeeds.** The
static-quality layer of `CLAUDE.md`'s mandatory-check list is in good standing —
an assumption that turned out to be true, which is worth recording precisely
because the session expected otherwise.

**B9 caveat:** the command as written in `CLAUDE.md` does not run on this
machine — there is no `python` on `PATH`, only `python3`. It exits 127, and in a
`&&` chain that is a hard stop; in a `;` chain it is a silently skipped check.
The validation itself passes when invoked correctly.

### B10 — preflight

```
PREFLIGHT  CareerVpCrudDevx · b587127 DIRTY · deployed commit unknown
           7 pass · 1 unknown · 1 fail   (553 resources across 6 stacks)

✔ stack healthy                 UPDATE_COMPLETE, last updated 2026-08-08 08:20:44
? deployed commit is known      CareerVpCrudDevx has no DeployedGitSha output
✔ api access logging enabled    1 stage(s) logging
✔ access logs kept long enough  ...ApiGatewayAccessLogGroup...: 7d
✔ stateful resources retained   18/18 Retain
✖ tables exist and are managed  10/10 table(s) exist but NO CloudFormation stack
                                owns them — a rebuild would not recreate them
✔ one code build per asset      2 asset group(s), largest has 31 functions
✔ llm api key present           /careervp/devx/anthropic-api-key exists
✔ sign-in callbacks configured  6 callback URL(s)
```

7/1/1 as predicted — **but the FAIL is the A11 false positive.** The true
position is 8 PASS / 1 UNKNOWN / 0 FAIL once line 366 is corrected. The one
genuine open item is the UNKNOWN, and W0.2 already has the fix for it.

The `DIRTY` stamp was caused by the sweep's own commands — see "Incidental
findings".

### B11 — `cdk diff CareerVpCrudDevx`

The command in the handoff fails as written:

```
$ cd infra && cdk diff CareerVpCrudDevx
No stacks match the name(s) CareerVpCrudDevx
```

`infra/app.py` only instantiates the devx stacks under `ENVIRONMENT=devx`
(documented at `src/backend/Makefile:130`). Correct invocation:

```
$ ENVIRONMENT=devx cdk list
CareerVpCrudDevx
CareerVpFrontend-Devx
$ ENVIRONMENT=devx cdk diff CareerVpCrudDevx --context p26_rehome_features=true
```

CDK created a real read-only change set, so the replacement information is
CloudFormation's own, not a template heuristic. Complete change inventory over
686 lines of diff:

```
  31 [~] AWS::Lambda::Function       (code asset S3Key only)
  19 [~] AWS::Lambda::Alias          (version pointer)
  19 [-] AWS::Lambda::Version        (superseded, "destroy")
  19 [+] AWS::Lambda::Version
   5 [~] AWS::CloudFormation::Stack  (nested TemplateURL)
   1 [~] AWS::IAM::Policy            (the K9 fix — ArtifactCleanupLambda → jobs table)
     [+] Output DeployedGitSha = "unstamped"
```

```
$ grep -E "AWS::DynamoDB::Table|AWS::S3::Bucket|AWS::Cognito::UserPool|AWS::ApiGateway::RestApi" B11.txt
>>> NONE <<<
```

**No table is created, replaced, or touched.** The 19 `destroy` markers are all
superseded `AWS::Lambda::Version` resources, which is normal CDK version
rotation. The single IAM change is exactly the K9 fix.

Independently, all 11 devx tables carry `DeletionProtectionEnabled: true`, so
even an erroneous replacement would be refused by DynamoDB.

**Handoff 03's B11 blocker is cleared.** The `[+] Output DeployedGitSha =
"unstamped"` in this diff is direct confirmation of W0.2: the diff was run
without `--context git_sha`, which is exactly what `create-changeset` does.

---

## Section C — environment and deploy reality

| # | Question | Result | Evidence |
|---|---|---|---|
| C1 | which CareerVp stacks exist | **PASS** | 3 root stacks + 10 nested: `CareerVpCrudDevx` (UPDATE_COMPLETE, updated 2026-08-08), `CareerVpCrudDev` (UPDATE_COMPLETE, 2026-07-30), `CareerVpCrudStaging` (UPDATE_COMPLETE, **2026-04-12**), `CareerVpFrontend-Dev` (2026-06-28) |
| C2 | is `CareerVpCrudStaging` current or abandoned? | **ABANDONED** | every staging Lambda `LastModified` = `2026-04-12T17:0x`; no `DeployedGitSha` output; **24 Lambdas vs devx's 31** |
| C3 | does a production stack exist? | **PASS — no** | no stack matching `CareerVp` in any active status is production |
| C4 | does GitHub env `production` exist? | **PASS — 404** | `gh api .../environments/production` → `{"message":"Not Found","status":"404"}`. 4 envs exist: `deploy-dev`, `dev`, `devx`, `staging` |
| C5 | which `make deploy` workflows actually deploy | **FAIL — 6 reachable, 5 ungated** | table below |
| C6 | do the devx tables have PITR? | **PASS — 10 of 11** | table below |
| C7 | log retention across Lambdas | **FAIL — 31 of 32 at 1 day** | below |

### C2 — staging is dead, and it is missing the features the journey needs

Present in devx, **absent in staging**:

```
careervp-ai-assist-lambda
careervp-artifact-cleanup-lambda          ← the K9 fix target
careervp-artifact-failure-handler-lambda
careervp-company-research-worker-lambda
careervp-cr-failure-handler-lambda
careervp-error-report-lambda
careervp-export-lambda                    ← journey step J9 is "export"
```

Both fleets run `python3.13`. Staging cannot serve the current journey; it should
be treated as a decommissioning decision, not a deploy target.

### C5 — the nine, adjudicated

The `dev` GitHub environment has **`"protection_rules":[]` and
`"deployment_branch_policy":null`** — it is not a gate. Any job declaring
`environment: dev` deploys unreviewed.

| Workflow | Trigger | Deploy condition | Env | Verdict |
|---|---|---|---|---|
| `company-research.yml` | push `feature/company-research` | `if: push \|\| dispatch+deploy` | `dev` (**ungated**) | **AUTO-DEPLOYS** |
| `cover-letter.yml` | push `feature/cover-letter` | same | `dev` (**ungated**) | **AUTO-DEPLOYS** |
| `cv-tailoring.yml` | push `feature/cv-tailoring`, `front/vpr-cv-fixes` | same | `dev` (**ungated**) | **AUTO-DEPLOYS** |
| `gap-analysis.yml` | push `feature/gap-analysis` | same | `dev` (**ungated**) | **AUTO-DEPLOYS** |
| `ui-upgrade-checks.yml` | push `ui-upgrade` (paths: frontend/backend/infra) | no `if` on the deploy job | `dev` (**ungated**) | **AUTO-DEPLOYS** (`STACK_NAME: CareerVpCrudDev`) |
| `db-redesign-checks.yml` | push `db-redesign` (same paths) | `needs: [ruff, mypy, pytest-unit]` | `dev` (**ungated**) | **AUTO-DEPLOYS** to devx via `make deploy-devx` |
| `gap-remediation.yml` | push `feature/gap-remediation` | `if: dispatch && inputs.deploy=='true'` | `gap-remediation` (**does not exist**) | dispatch-only |
| `pr-serverless-service.yml` | PR to `main`, dispatch | `if: dispatch` | `dev` (**ungated**) | dispatch-only |
| `main-serverless-service.yml` | `workflow_dispatch` only | — | `staging` (gated) → `production` (**does not exist**) | dispatch-only |
| `deploy-staging.yml` | — | comment only, no execution | — | no-op |

**Six workflows reach a real deploy; five of the six auto-deploy on a branch
push with no human gate.** `db-redesign-checks.yml` is the only one that runs
lint/type/tests first, and it targets devx — the same stack `make journey`
measures, so a push to `db-redesign` silently moves the journey's ground truth.

Two more, not in the handoff's frame:

- **`main-serverless-service.yml` declares `environment: production`, and that
  environment does not exist.** GitHub auto-creates a missing environment on
  first use **with no protection rules**. The one path in this repo that says
  "production" is therefore ungated by construction. It is `workflow_dispatch`
  only, so nothing fires it accidentally — but the gate people believe is there
  is not.
- `gap-remediation.yml`'s `gap-remediation` environment has the same property.

### C6 — PITR, all 11 tables

```
careervp-applications-table-devx               PITR=ENABLED    DeletionProtection=True
careervp-artifacts-table-devx                  PITR=ENABLED    DeletionProtection=True
careervp-company-research-cache-table-devx     PITR=ENABLED    DeletionProtection=True
careervp-cvs-table-devx                        PITR=ENABLED    DeletionProtection=True
careervp-gap-responses-table-devx              PITR=ENABLED    DeletionProtection=True
careervp-idempotency-table-devx                PITR=ENABLED    DeletionProtection=True
careervp-identity-map-table-devx               PITR=ENABLED    DeletionProtection=True
careervp-jobs-table-devx                       PITR=ENABLED    DeletionProtection=True
careervp-knowledge-table-devx                  PITR=ENABLED    DeletionProtection=True
careervp-llm-cache-devx                        PITR=DISABLED   DeletionProtection=True
careervp-users-table-devx                      PITR=ENABLED    DeletionProtection=True
```

10 of 11 ENABLED. The exception is `careervp-llm-cache-devx`, a regenerable
cache — defensible, but it is a deliberate-looking choice nobody recorded.
**Note the count: 11 tables, not 10.** The "10" everywhere in the plan comes
from preflight's `wanted` set, which is derived from Lambda `TABLE`-suffixed env
vars; `llm-cache` is reached by another name and never appears.

### C7 — log retention is the worst finding in this section

```
$ per-Lambda retentionInDays across all 32 devx functions
   1 None   (CDK-internal BucketNotificationsHandler)
  31 1      ← every application Lambda
```

Source: `infra/careervp/api_construct.py` declares
`retention=logs.RetentionDays.ONE_DAY` **34 times**; only two declarations in
the whole infra tree use `ONE_WEEK` (`api_construct.py:472`,
`artifact_chain_construct.py:308`).

The handoff's premise was "gap-api is 1 day." It is **every application Lambda**.
`preflight.py` sets `MIN_LOG_RETENTION_DAYS = 7` and checks it — but only
against the API Gateway access log group, which is at 7d and passes. Lambda
retention is not a preflight premise at all.

If handoff 03 deploys and the journey moves from 3, the logs explaining why are
gone within 24 hours.

---

## Section D — scope reality

| # | Question | Result | Evidence |
|---|---|---|---|
| D1 | Company Research reachable and covered? | **PASS** | page `app/applications/[id]/company-research/page.tsx`; reachable via `app/applications/[id]/page.tsx:243` `router.push(\`/applications/${jobId}/company-research\`)`; covered by `tests/ui/unit/CompanyResearchPage.test.tsx` + 8 more unit/integration files. **No e2e spec.** |
| D2 | Knowledge Base reachable and covered? | **FAIL** | **zero** frontend files reference it (`grep -rln 'knowledge\|Knowledge' app components lib hooks` → 0). No route, no page, no component, no frontend test. |
| D3 | Hebrew / RTL functional? | **PARTIAL — 6 of 24 pages, no e2e** | below |
| D4 | billing / trial / subscription covered end to end? | **FAIL** | below |
| D5 | ToS / Privacy Policy pages exist? | **FAIL — neither** | no file under `src/frontend` matches `terms\|privacy\|legal\|tos` (the four hits are `ArtifactAu**tos**ave*`) |
| D6 | `STRIPE_SECRET_KEY` from a secret store? live/test guard? | **FAIL on both** | below |

### D2 — Knowledge Base is a V1 feature with no product

- No frontend surface of any kind.
- `src/backend/careervp/handlers/knowledge_base_handler.py` exists with
  `lambda_handler`, `_handle_get`, `_handle_post` — and is **referenced by
  nothing in `infra/`**. It is wired to no route and no Lambda. Dead code.
- The only `/knowledge-base` route is
  `api_construct.py:3512: ("/knowledge-base", "GET", self.company_research_func)`
  — served by the **Company Research** Lambda, not the knowledge handler. The
  `_handle_post` path is unreachable from the API entirely.

This is a W3.1 `dead_api.py` finding arriving early: a handler with no route, and
a route pointing at a different feature's Lambda.

### D3 — Hebrew is real but thin, and unverifiable

There *is* a working hand-rolled implementation — better than "a TODO stub" implied:

```tsx
const locale = new URLSearchParams(window.location.search).get('locale');
if (locale?.toLowerCase().startsWith('he')) return 'he';
...
<div data-testid="tailored-cvs-page" dir={locale === 'he' ? 'rtl' : 'ltr'}>
```

But:

- **6 of 24 pages** implement it: `tailored-cvs`, `applications/new`,
  `gap-analysis`, `cv-center`, `cover-letters`, `billing`. Dashboard, the
  applications hub, VPR, interview-prep, company-research, settings and all five
  auth pages have no locale handling.
- **No i18n dependency** in `package.json`. Copy lives in per-page `TEXT[locale]`
  literals — six independent translation tables with nothing keeping them in sync.
- **`app/layout.tsx:17` hardcodes `<html lang="en">`** and never switches. Screen
  readers and browser tooling are told the Hebrew pages are English.
- Unit tests do exercise the `he` path (10 files under `tests/ui/unit/`), so it
  is not unverified — but the **only e2e case is the TODO stub** at
  `cv-center.spec.ts:179`, which navigates to `/cv-center?locale=he` and asserts
  nothing.

### D4 — the purchase path is not covered, and the tests that claim to are tautologies

`npm run test:e2e` = `jest --selectProjects e2e`. Five billing files, dense-looking:

| File | Tests | `expect(` |
|---|---|---|
| `checkout-to-active.e2e.test.ts` | 2 | 11 |
| `upgrade-flow.e2e.test.ts` | 2 | 11 |
| `subscription-cancellation.e2e.test.ts` | 2 | 9 |
| `invoice-past-due.e2e.test.ts` | 2 | 6 |
| `invoice-recovery.e2e.test.ts` | 2 | 6 |

43 assertions. Every one asserts a value the same test set three lines earlier:

```ts
mockApi.post.mockResolvedValue({
  status: 200,
  data: { checkout_url: 'https://checkout.stripe.com/c/pay/cs_test_e2e_001' },
});
const checkoutResponse = await mockApi.post(`${DEV_API_BASE}/billing/checkout`, {...});
expect(checkoutResponse.status).toBe(200);
expect(checkoutResponse.data.checkout_url).toMatch(/checkout\.stripe\.com/);
```

No product code executes. `DEV_API_BASE` is a string that is never dialled.

This is a **worse** hollowness class than the Playwright specs the plan already
catalogued: those are visibly empty, so they read as unfinished. These read as
finished, and an assertion count would score them as the best-covered area in
the repo.

**Answer to D4: no. The billing / trial / subscription flow is covered end to
end nowhere.** Combined with D6, the flow that takes money has never executed
against a payment provider in any environment.

### D6 — Stripe

```
$ aws lambda get-function-configuration --function-name careervp-billing-lambda-devx
{
  "PAYMENT_PROVIDER": "mock",
  "PRICE_ID_MONTHLY": "price_placeholder_monthly",
  "PRICE_ID_QUARTERLY": "price_placeholder_quarterly",
  "PAYMENT_PROVIDER_WEBHOOK_SECRET_SSM_PARAM": "/careervp/devx/payment-provider-webhook-secret",
  ...
}
```

- **`STRIPE_SECRET_KEY` is set nowhere** — not in the Lambda environment, not in
  `infra/`, not in any workflow. `stripe_provider.py:223` reads
  `os.environ.get('STRIPE_SECRET_KEY')` and raises
  `PAYMENT_PROVIDER_CONFIGURATION_ERROR` when absent. It fails closed, which is
  the right failure, but it means Stripe has never run.
- **Not from a secret store.** Note the contrast in the same Lambda: the webhook
  secret *is* sourced from SSM. The pattern exists and was simply not applied to
  the API key.
- **No live/test guard.** Nothing anywhere inspects `sk_live_` vs `sk_test_`.
  The key is read from the environment and sent to
  `https://api.stripe.com/v1` as-is. Whichever key is present is the key used.
- Price IDs are literal placeholders.

**D4 + D6 together:** `N = 9` can be reached with the purchase path mocked,
placeholder-priced, keyless and untested. The project's definition of done does
not include the only flow that produces revenue.

---

## Section E — prediction ledger

### E1 — the explicit recorded predictions

| # | Predicted | Observed | Verdict |
|---|---|---|---|
| B1 | `606 failed, 1100 passed, 54 skipped, 4 xfailed, 100 errors` | `606 failed, 1100 passed, 54 skipped, 4 xfailed, 100 errors` | **HELD** |
| B2 unit | 1430p / 0f | 1430 passed, 15 skipped, 4 xfailed, 0 failed | **HELD** |
| B2 integration | 185p / 11f | 11 failed, 185 passed, 17 skipped | **HELD** |
| B2 infrastructure | 81p / 1f | 1 failed, 81 passed, 1 skipped | **HELD** |
| B2 regression | 37p / 2f | 2 failed, 37 passed | **HELD** |
| B2 e2e | 4p / 21s | 4 passed, 21 skipped | **HELD** |
| B2 models | 18p | 18 passed | **HELD** |
| B2 security | 3p | 3 passed | **HELD** |
| B2 infra | 34p | 34 passed | **HELD** |
| B3 | `85 suites, 429 passed, 26 skipped, 0 failed` | exactly that | **HELD** |
| B4 | `67 files, 739 passed, 0 failed` | exactly that | **HELD** |
| B10 | 7 PASS / 1 UNKNOWN / 1 FAIL | 7 PASS / 1 UNKNOWN / 1 FAIL | **HELD** |

**12 of 12 — 100%.** B5–B9 carried no prediction by design.

### E2 — the inherited and structural premises

| Premise | Source | Observed | Verdict |
|---|---|---|---|
| `deploy-dev` env + reviewer | 09-14 handoff | confirmed | **HELD** |
| `staging` env + reviewer | 09-14 handoff | confirmed | **HELD** |
| both branch-restricted | 09-14 handoff | confirmed | **HELD** |
| `careervp-review-readonly` exists | 09-14 handoff | confirmed | **HELD** |
| policy is read-only | 09-14 handoff | confirmed | **HELD** |
| harness Cognito account | 09-14 handoff | confirmed | **HELD** |
| harness subscription active | 09-14 handoff | confirmed | **HELD** |
| replacement report flags protected types | 09-14 handoff | blind to `GlobalTable` | **MISSED** |
| four fixes have failing-without tests | commit messages | 4 of 4 RED | **HELD** |
| P-05 attacks as a real tenant | commit `1f71fbd` | confirmed | **HELD** |
| no production stack | program plan | none | **HELD** |
| no `production` GitHub env | program plan | 404 | **HELD** |
| staging ~5 months stale | program plan | frozen 2026-04-12, 7 Lambdas missing | **HELD** |
| 9 workflows run ungated `make deploy` | program plan | 6 reachable, 5 auto-deploy ungated | **MISSED** (overstated) |
| **10 of 10 devx tables unmanaged by CFN** | preflight | **11 of 11 are managed** | **MISSED — false** |
| `cdk diff` may try to create 10 tables | program plan | zero table changes | **MISSED** (safe direction) |
| PITR spot-checked on 2 of 10 | 09-20 handoff | 10 of 11 ENABLED, 1 DISABLED | **MISSED** (understated) |
| gap-api log retention is 1 day | program plan | **31 of 32 Lambdas** at 1 day | **MISSED** (understated) |
| no ToS / Privacy pages | 09-20 handoff | neither exists | **HELD** |

**13 HELD / 6 MISSED — 68%.**

### E3 — combined, and the recommendation

**25 HELD / 6 MISSED across 31 scored rows — 81%.**

Above the ~70% bar, so **do not re-ground the plan**. But the hit rate is not the
interesting number, and reporting only the headline would be the mistake this
section exists to prevent:

- **Everything measured about the test estate held perfectly — 12 of 12.** The
  planning session's numbers are trustworthy, reproduced to the digit.
- **Every miss is in the deploy-safety layer, and it is the same miss twice.**
  The `GlobalTable` vs `Table` type name defeats both `preflight.py` and
  `changeset_replacement_report.py`, producing one false alarm (tables are fine)
  and one silent hole (the gate cannot fire). A plan built on those two outputs
  was reading a broken instrument in both directions.
- The remaining misses (C6, C7, C5) are all **the same shape**: a figure
  established by spot-check and then carried forward as if enumerated. Two of
  three understate the problem by an order of magnitude.

The recommendation is not to re-ground, it is narrower: **W0 gains a fourth item
— fix the two type-name blind spots — and it should land before handoff 03's
step 4, because that step's entire safety argument rests on the gate that cannot
currently fire.**

---

## Incidental findings (found while measuring, not on any checklist)

1. **Running the mandatory checks dirties the tree.** After a clean commit, the
   sweep produced:
   ```
   M docs/beta/evidence/I2_persistence/persistence-roundtrip-report.json
   M docs/beta/evidence/I3_auth/auth-abuse-matrix.json
   M src/frontend/dist/tsconfig.tsbuildinfo
   ```
   `tests/integration/test_l2_auth_integration.py` and
   `test_l1_phase_integration.py` rewrite tracked evidence files (timestamp
   only), and `npm run typecheck` rewrites a **committed build artifact**.
   Per `HARNESS.md` a `git_dirty` proof is non-reproducible — so it is currently
   impossible to run `CLAUDE.md`'s mandatory checks and then record an
   admissible proof. This is the structural cause of the 09-14 journey proof's
   `git_dirty: true`, not carelessness. `dist/tsconfig.tsbuildinfo` should be
   gitignored; the two evidence writers should target a temp path.

2. **`preflight.py` truncates the first character of the first dirty path.**
   `"ocs/beta/evidence/I2_persistence/..."` — the leading `d` is gone. Cosmetic
   in the report, but `dirty_paths` is proof data.

3. **`cdk synth` reports 493 of 600 resources on `CareerVpCrudDev`** (`INFO
   Number of resources: 493 is approaching allowed maximum of 600`). Devx is at
   553 across 6 stacks per preflight. Not urgent; it is a ceiling W2's table work
   will push against.

4. **`deploy-staging.yml` declares `STACK_NAME=CareerVpCrudStaging` but the
   `make deploy` path hardcodes `CareerVpCrudDev`** — documented as a known
   pre-existing mismatch at `src/backend/Makefile:122-128`. Recorded here so it
   is visible outside a Makefile comment.

5. **`CLAUDE.md`'s naming-check command is broken** (`python` → 127). See B9.
