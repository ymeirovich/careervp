{
  "handoff": "03-ci-proof-and-deploy-gates",
  "doc": "docs/handoff/2026-09-20-HANDOFF-03-ci-proof-and-deploy-gates.md",
  "git_sha": "d7dc6cddb7d3c504fca5af8bbaaa1e5c865103d8",
  "git_dirty": true,
  "branch": "tools/proof-harness",
  "timestamp_utc": "2026-09-20T15:49:32Z",
  "model": "Opus 5 (1M context). DEVIATION: the handoff specified 'Sonnet 5, high effort' with escalation to Opus 5 only if Step 1 showed a CI-vs-local difference. The session model was Opus 5 throughout and was not switched down; per CLAUDE.md a model switch must be confirmed via /status, which this session could not do. Recorded rather than hidden. Step 1 did in fact surface a CI-environment finding that the handoff did not anticipate.",
  "git_dirty_adjudication": {
    "summary": "TRUE, but for ONE reason only, and it is materially smaller than handoff 02's. The test-generated churn group is GONE — closed by this handoff's Step 3. What remains is the same three prior-session handoff docs the user elected to handle personally.",
    "remaining_dirty_paths": [
      "docs/handoff/2026-09-19-HANDOFF-01-ci-wiring.md",
      "docs/handoff/2026-09-19-PRODUCTION-PROGRAM.md",
      "docs/handoff/2026-09-20-HANDOFF-00-measurement-sweep.md"
    ],
    "note": "Already dirty when handoff 02 started and still uncommitted. Not authored or touched by this session. Explicitly NOT swept in: per the handoff's ground rules, safe_commit.sh was not used (it runs `git add -A`); every commit used explicit `git add <paths>` plus `pre-commit run --files`.",
    "affects_measurements": false,
    "what_changed_vs_handoff_02": "Handoff 02 recorded two dirty groups. Group 2 (docs/beta/evidence/I{1,2,3}_*/*.json, regenerated by every suite run) no longer appears. Proven: `git checkout --` the three files, re-run the three integration tests, `git status --short docs/beta/` returns empty.",
    "reproducibility": "Check out d7dc6cd clean and re-run the commands in proof_obligations. The three dirty paths do not participate in any measurement recorded here."
  },
  "commits_this_handoff": [
    {
      "sha": "b638b35",
      "subject": "ci(workflows): gate the two auto-deploy workflows behind devx",
      "concern": "Step 2 / W5"
    },
    {
      "sha": "cb279d7",
      "subject": "test(evidence): write beta evidence to a temp path, not the working tree",
      "concern": "Step 3"
    },
    {
      "sha": "d7dc6cd",
      "subject": "chore(git): stop tracking the frontend TypeScript build artifact",
      "concern": "Step 3"
    }
  ],
  "step0_verification_of_handoff_02": {
    "verdict": "ALL EIGHT REPRODUCE. Handoff 02's chain link is sound. Commands were run before reading its prose, per the plan's obligation 1.",
    "checks": [
      {
        "id": "0.1",
        "command": "uv run pytest --collect-only -q | tail -1",
        "recorded": "2118 tests collected",
        "observed": "2118 tests collected in 3.26s",
        "verdict": "MATCH"
      },
      {
        "id": "0.2",
        "command": "uv run pytest -q --tb=no -p no:cacheprovider -p no:randomly | tail -1",
        "recorded": "2058 passed, 48 skipped, 12 xfailed",
        "observed": "2058 passed, 48 skipped, 12 xfailed, 139 warnings in 209.88s",
        "verdict": "MATCH — 0 failed, 0 errors"
      },
      {
        "id": "0.3a",
        "command": "uv run pytest -q --tb=no -p no:cacheprovider | tail -1 (random order)",
        "recorded": "identical to 0.2",
        "observed": "2058 passed, 48 skipped, 12 xfailed, 141 warnings in 236.00s",
        "verdict": "MATCH"
      },
      {
        "id": "0.3b",
        "command": "same, second random seed",
        "recorded": "identical to 0.2",
        "observed": "2058 passed, 48 skipped, 12 xfailed, 139 warnings in 242.26s",
        "verdict": "MATCH"
      },
      {
        "id": "0.4",
        "command": "uv run pytest tests/infrastructure/test_k9_artifact_cleanup_env.py tests/infrastructure/test_p02_billing_reconcile_entrypoint.py -q -p no:randomly",
        "recorded": "3 passed",
        "observed": "3 passed in 10.66s",
        "verdict": "MATCH"
      },
      {
        "id": "0.5",
        "command": "grep -rc 'sys\\.modules\\.pop|del sys\\.modules' tests/ | grep -v ':0' | grep -v import_isolation",
        "recorded": "empty",
        "observed": "empty",
        "verdict": "MATCH"
      },
      {
        "id": "0.6",
        "command": "uv run pytest tests/regression/test_careervp_import_isolation.py -q",
        "recorded": "256 passed",
        "observed": "256 passed in 6.51s",
        "verdict": "MATCH"
      },
      {
        "id": "0.7",
        "command": "the 8-directory loop",
        "recorded": "0 failed / 8; infrastructure 82 passed 1 skipped; regression 294 passed 1 xfailed",
        "observed": "unit 1430 passed 15 skipped 4 xfailed | integration 193 passed 11 skipped 7 xfailed | infrastructure 82 passed 1 skipped | e2e 4 passed 21 skipped | models 18 passed | regression 294 passed 1 xfailed | security 3 passed | infra 34 passed",
        "verdict": "MATCH — 0 failed / 0 errors across all 8"
      },
      {
        "id": "0.8",
        "command": "uv run mypy careervp --strict | tail -1",
        "recorded": "Success: no issues found in 137 source files",
        "observed": "Success: no issues found in 137 source files",
        "verdict": "MATCH"
      }
    ],
    "no_drift": "0.1 and 0.7 were expected to drift if anyone had added tests. Neither drifted; nothing landed on the branch between 9134ff8 and 8e8c9cf except the handoff-03 document itself."
  },
  "step1_real_actions_run": {
    "status": "PARTIALLY PAID — a real Actions run was observed for the first time in this chain, but NOT the one the handoff asked for. The reason is measured, not guessed.",
    "step1a_push_blocker": {
      "resolution_chosen_by_user": "Allow via GitHub's unblock URL (the web flow). Rewrite-history was offered and declined.",
      "blocker_reproduced": "Identical to handoff 02's record: GH013, Amazon AWS Access Key ID, commit 51129b1c3bcf1c15457d617a1807ed5823db5269, path docs/evidence/astra-pass-b/iam-authorization-details.json:15.",
      "secret_still_present_at_head": "Confirmed without printing it: `sed -n '15p' <file> | grep -cE 'AKIA[0-9A-Z]{16}'` -> 1; the same pattern count in docs/evidence/sweep-00-20260920T082517-b587127.md -> 1.",
      "not_bypassed": "Push protection was NOT overridden by this session. The unblock was performed by the user in the GitHub web UI.",
      "user_reported_404_on_first_attempt": "Diagnosed rather than retried blindly: the unblock URL was regenerated by a second push and was byte-identical, proving it had not expired. GitHub returns 404 (not 403) on /security/ pages to unauthorized viewers, so the cause was the browser session, not the link. The user then signed in and approved.",
      "rotation": "UNCHANGED AND OUTSTANDING. The key ID has been in the repository since 2026-09-14 and is now on a PUBLIC remote. Rotation is the user's action, not this session's.",
      "push_result": "SUCCEEDED. `git push -u origin tools/proof-harness` -> [new branch] tools/proof-harness. The branch existed only locally until this handoff."
    },
    "pre_push_safety_check_re_derived": {
      "why_re_derived": "The handoff instructed re-running handoff 02's analysis rather than trusting it, since workflows may have changed. Re-derived from scratch by parsing all 28 workflow YAMLs with PyYAML, not by reading handoff 02's conclusion.",
      "method": "For every workflow: normalise the `on:` key (PyYAML parses bare `on:` as boolean True), fnmatch the push branch filters against 'tools/proof-harness', and for every job whose step `run:` bodies match /make deploy|cdk deploy|sam deploy|execute-change-set|aws s3 sync|amplify publish|serverless deploy/, record the job `if:`, `needs:` and `environment:`.",
      "improvement_over_grep": "A plain grep for 'make deploy' flags deploy-staging.yml, but its only match is a COMMENT on line 30. The parsed audit correctly excludes it — deploy-staging.yml's real path is create-change-set + a separate execute job gated on `environment: staging`.",
      "push_result": "Only security.yml matches a push of this branch (it triggers on all branches). It contains no deploy step. CONFIRMED EMPIRICALLY, not just statically: the actual push produced exactly one run — 35520223998, Security Audit — and nothing else.",
      "pull_request_result": "Seven PR-triggered workflows contain deploy commands; none can deploy on a pull_request event. company-research/cover-letter/cv-tailoring/gap-analysis gate on `push || (workflow_dispatch && inputs.deploy=='true')`; gap-remediation on workflow_dispatch only; pr-serverless-service gates the deploy STEP on workflow_dispatch; deploy-vpr-async's execute job `needs:` a create job gated on `push && ref==main`, so it is skipped transitively.",
      "conclusion": "Deploy-safe. Confirmed by the run list after the fact: zero deploy workflows fired."
    },
    "the_finding": {
      "headline": "pr-validation.yml did not run, and cannot run, because PR #221 is CONFLICTING.",
      "chain": [
        "Push succeeded; PR #221 opened against main.",
        "gh pr checks 221 returned exactly five checks: auth-enforcement, code-security, iac-security, node-security, python-security. All five belong to security.yml.",
        "Those five arrived from the PUSH event (run 35520223998), not from the PR. GitHub surfaces push-triggered check runs on a PR when the head SHA matches, which makes an empty PR gate look populated.",
        "gh run list --branch tools/proof-harness --event pull_request returned ZERO runs.",
        "gh workflow list --all shows 'PR Validation' as active, and its committed on-block is `pull_request: types: [opened, synchronize, reopened]` with no branch or paths filter.",
        "gh pr view 221 -> mergeable: CONFLICTING, mergeStateStatus: DIRTY.",
        "GitHub does not build refs/pull/<n>/merge for a conflicting PR. pull_request-triggered workflows run on that merge ref. No merge ref, no run."
      ],
      "second_independent_test": "Pushing this handoff's three commits fired a `synchronize` event. pull_request run count stayed at 0 across six subsequent polls while mergeable stayed CONFLICTING. The hypothesis survived a second trigger type.",
      "the_conflict": {
        "conflicting_paths": [
          "docs/db-redesign/code/code-analysis/project/runbooks/wave-1-status.md"
        ],
        "kind": "add/add",
        "everything_else": "auto-merges clean (git merge-tree --write-tree --name-only origin/main HEAD)",
        "scope_of_a_merge": "main is 10 commits ahead: 16 files, +2779/-30, of which 4 are under src/backend (careervp/handlers/artifact_dependency_utils.py, careervp/handlers/company_research_worker_handler.py and their two unit test files)."
      },
      "why_this_matters": "Two handoffs recorded this proof as 'deferred, blocked on push protection'. That was only the first of two blockers. The gate wired in handoff 01 has never run because the PR that would exercise it cannot be built — a single markdown file has been holding the entire backend and frontend CI gate shut. Reading the YAML could never have found this; only reading the run could.",
      "user_decision": "The user deferred the resolve/rebase/stop decision to the end of the session and asked for pros and cons first. Nothing was merged or rebased. PR #221 is left OPEN and CONFLICTING."
    },
    "what_was_actually_observed": {
      "run_id": 35520223998,
      "workflow": "Security Audit (security.yml)",
      "event": "push",
      "conclusion": "failure",
      "jobs": {
        "code-security": "success",
        "iac-security": "success",
        "auth-enforcement": "success",
        "node-security": "success",
        "python-security": "failure"
      },
      "failure_captured_verbatim": "Found 23 known vulnerabilities in 4 packages — anyio 4.12.1 (CVE-2026-63374, CVE-2026-64847 -> 4.14.2); cryptography 46.0.5 (PYSEC-2026-35 -> 46.0.6, PYSEC-2026-36 -> 46.0.7, PYSEC-2026-3552 -> 50.0.0, PYSEC-2026-3553 -> 49.0.0, PYSEC-2026-3554 -> 49.0.0, GHSA-537c-gmf6-5ccf -> 48.0.1); pypdf 6.14.2 (PYSEC-2026-3655/3656 -> 6.15.0, PYSEC-2026-3910/3911 -> 6.16.1, PYSEC-2026-3912 -> 6.15.0, PYSEC-2026-3913 -> 6.16.0); soupsieve 2.8.4 (CVE-2026-85999, CVE-2026-86000 -> 2.9.0). ##[error]Process completed with exit code 1.",
      "note": "NEW finding. No prior handoff or sweep recorded that the only workflow which runs on a branch push is red. This is a real observed CI result and it is unrelated to the pytest gate."
    }
  },
  "step2_w5_deploy_gating": {
    "environment_gating_measured": {
      "command": "gh api repos/:owner/:repo/environments --jq '.environments[] | {name, protection_rules}'",
      "result": "READ SUCCESSFULLY — this is a PASS/FAIL measurement, not UNKNOWN.",
      "environments": {
        "deploy-dev": {
          "protection_rules": [
            "required_reviewers",
            "branch_policy"
          ],
          "branch_policy": [
            "main"
          ],
          "verdict": "GATED"
        },
        "dev": {
          "protection_rules": [],
          "verdict": "UNGATED — sweep-00 finding #5 CONFIRMED, still open"
        },
        "devx": {
          "protection_rules": [
            "required_reviewers"
          ],
          "branch_policy": null,
          "verdict": "GATED, and until this handoff was referenced by NO workflow"
        },
        "staging": {
          "protection_rules": [
            "required_reviewers",
            "branch_policy"
          ],
          "branch_policy": [
            "develop",
            "main"
          ],
          "verdict": "GATED"
        }
      },
      "environments_that_do_not_exist": {
        "names": [
          "production",
          "gap-remediation"
        ],
        "referenced_by": [
          "main-serverless-service.yml (staging + production jobs)",
          "gap-remediation.yml (deploy-sandbox job)"
        ],
        "consequence": "GitHub auto-creates a referenced environment on first use with NO protection rules. Both are therefore effectively ungated, though both workflows are workflow_dispatch-only today.",
        "status": "NEW finding, not in sweep-00."
      },
      "secrets_and_variables": "All four existing environments hold zero secrets and zero variables (gh api .../secrets and .../variables). Relabelling an environment therefore cannot break credential resolution — this was measured before editing, not assumed."
    },
    "sharpest_edge_confirmed_and_sharpened": "db-redesign-checks.yml runs `make deploy-devx` -> CareerVpCrudDevx (Makefile:133-137), the stack `make journey` measures — but declared `environment: dev`, which has no protection. The gated `devx` environment already existed and was wired to nothing. The protection was on the wrong name.",
    "fix_applied": {
      "commit": "b638b35",
      "option_chosen_by_user": "Repoint the two auto-deployers at the already-gated `devx` environment.",
      "options_costed_for_the_user": [
        "Required reviewers on `dev` — one change, gates all six workflows. NOT chosen.",
        "Repoint the two auto-deployers at `devx` — 2-line change, gates the two that deploy with no `if:` at all. CHOSEN.",
        "Edit the `if:` conditions in nine workflows. NOT chosen.",
        "Accept and change nothing. NOT chosen."
      ],
      "why_devx_and_not_deploy_dev": "MEASURED: `deploy-dev` carries a custom deployment branch policy allowing only `main`. Pointing a db-redesign/ui-upgrade push at it would BLOCK the job outright rather than gate it. `devx` has branch_policy: null, so it gates without blocking.",
      "changed": [
        ".github/workflows/db-redesign-checks.yml: deploy-backend-dev environment dev -> devx; job name corrected to 'Deploy backend -> devx' since it deploys CareerVpCrudDevx",
        ".github/workflows/ui-upgrade-checks.yml: deploy-backend-dev environment dev -> devx; job name left as 'Deploy backend -> dev' because `make deploy` targets CareerVpCrudDev (Makefile:142). An inline comment records that the environment is the APPROVAL GATE, not the target stack, so the mismatch is documented rather than silent."
      ]
    },
    "full_deploy_surface_after_the_change": [
      {
        "workflow": "company-research.yml",
        "push_branches": [
          "feature/company-research"
        ],
        "job": "deploy-dev",
        "gate": "push || (workflow_dispatch && inputs.deploy=='true')",
        "environment": "dev",
        "verdict": "UNGATED ON PUSH — explicitly accepted by the operator"
      },
      {
        "workflow": "cover-letter.yml",
        "push_branches": [
          "feature/cover-letter"
        ],
        "job": "deploy-dev",
        "gate": "push || (workflow_dispatch && inputs.deploy=='true')",
        "environment": "dev",
        "verdict": "UNGATED ON PUSH — explicitly accepted by the operator"
      },
      {
        "workflow": "cv-tailoring.yml",
        "push_branches": [
          "feature/cv-tailoring",
          "front/vpr-cv-fixes"
        ],
        "job": "deploy-dev",
        "gate": "push || (workflow_dispatch && inputs.deploy=='true')",
        "environment": "dev",
        "verdict": "UNGATED ON PUSH — explicitly accepted by the operator"
      },
      {
        "workflow": "gap-analysis.yml",
        "push_branches": [
          "feature/gap-analysis"
        ],
        "job": "deploy-dev",
        "gate": "push || (workflow_dispatch && inputs.deploy=='true')",
        "environment": "dev",
        "verdict": "UNGATED ON PUSH — explicitly accepted by the operator"
      },
      {
        "workflow": "db-redesign-checks.yml",
        "push_branches": [
          "db-redesign"
        ],
        "job": "deploy-backend-dev",
        "gate": "no `if:` at all",
        "environment": "devx (was dev)",
        "verdict": "CLOSED by b638b35 — now requires a reviewer"
      },
      {
        "workflow": "ui-upgrade-checks.yml",
        "push_branches": [
          "ui-upgrade"
        ],
        "job": "deploy-backend-dev",
        "gate": "no `if:` at all",
        "environment": "devx (was dev)",
        "verdict": "CLOSED by b638b35 — now requires a reviewer"
      },
      {
        "workflow": "deploy.yml",
        "push_branches": [
          "main"
        ],
        "job": "execute-change-set-dev",
        "gate": "github.ref == 'refs/heads/main'",
        "environment": "deploy-dev",
        "verdict": "GATED (required reviewers + main-only branch policy)"
      },
      {
        "workflow": "deploy-vpr-async.yml",
        "push_branches": [
          "main"
        ],
        "job": "execute-change-set-vpr-async",
        "gate": "needs create-change-set (push && ref==main)",
        "environment": "deploy-dev",
        "verdict": "GATED"
      },
      {
        "workflow": "deploy-staging.yml",
        "push_branches": [
          "develop"
        ],
        "job": "execute-change-set-staging",
        "gate": "needs create-change-set-staging",
        "environment": "staging",
        "verdict": "GATED"
      },
      {
        "workflow": "main-serverless-service.yml",
        "push_branches": null,
        "job": "staging, production",
        "gate": "workflow_dispatch only",
        "environment": "staging / production",
        "verdict": "staging GATED; production environment DOES NOT EXIST -> would be auto-created ungated"
      },
      {
        "workflow": "gap-remediation.yml",
        "push_branches": [
          "feature/gap-remediation"
        ],
        "job": "deploy-sandbox",
        "gate": "workflow_dispatch && inputs.deploy=='true'",
        "environment": "gap-remediation (does not exist)",
        "verdict": "gate is on the `if:`; environment would be auto-created ungated"
      },
      {
        "workflow": "pr-serverless-service.yml",
        "push_branches": null,
        "job": "tests",
        "gate": "deploy STEP gated on workflow_dispatch",
        "environment": "dev",
        "verdict": "cannot deploy on a pull_request event"
      },
      {
        "workflow": "test-changeset.yml",
        "push_branches": null,
        "job": "changeset-test",
        "gate": "workflow_dispatch only",
        "environment": null,
        "verdict": "creates a change set, does not execute"
      }
    ],
    "why_the_existing_test_could_not_catch_this": "tests/infra/test_p28_deploy_identity.py:95 asserts that deploy.yml's execute jobs declare `environment:` — and its own comment says '(required reviewer configured in GitHub settings)'. A static test cannot see whether that environment actually has protection rules. `dev` satisfied the assertion while having none. The invariant is only half-checkable from the repo."
  },
  "step3_churn_fixes": {
    "beta_evidence_writers": {
      "commit": "cb279d7",
      "sites_redirected": [
        "tests/integration/test_l0_phase_integration.py -> I1_generators/generator-output-audit.json",
        "tests/integration/test_l1_phase_integration.py -> I2_persistence/persistence-roundtrip-report.json",
        "tests/integration/test_l2_auth_integration.py -> I3_auth/auth-abuse-matrix.json",
        "tests/integration/test_l2_auth_integration.py -> I4_identity/identity-extraction-audit.txt (NOT named by the handoff; same hazard, same file, included deliberately — it escaped notice only because its payload carries no timestamp today)"
      ],
      "mechanism": "BETA_EVIDENCE_ROOT = Path(os.environ.get('CAREERVP_BETA_EVIDENCE_DIR', tempfile.gettempdir())). Default is a temp dir; the committed artifacts can still be refreshed deliberately by setting the variable.",
      "safety_argument": "Each test writes then asserts on what it just wrote; no test reads pre-existing content. Verified at test_l0:353, test_l1:74, test_l2:118-119 and :175.",
      "proof": "git checkout -- the three tracked files; uv run pytest the three test files -> '4 passed in 1.12s'; git status --short docs/beta/ -> EMPTY."
    },
    "tsbuildinfo": {
      "commit": "d7dc6cd",
      "finding": "src/frontend/dist/tsconfig.tsbuildinfo was the ONLY tracked file under src/frontend/dist/ (git ls-files src/frontend/dist | wc -l -> 1), and tsconfig has incremental: true with outDir ./dist, so `npm run typecheck` rewrote it every run.",
      "fix": "Added src/frontend/dist/ to .gitignore; git rm --cached the file.",
      "proof": "npm run typecheck -> clean (tsc --noEmit, no errors); git status --short src/frontend/ shows only the staged deletion, no regenerated artifact."
    },
    "net_effect": "The two automatic sources of working-tree churn are gone. For the first time in this chain, running the mandatory checks leaves the tree clean. The only remaining dirty paths are three prior-session documents the user owns."
  },
  "proof_obligations": [
    {
      "claim": "CI ran the 8 backend directories",
      "command": "gh run view <id> --log | grep -c '::group::tests/'",
      "before": "never run",
      "target": "8",
      "result": "NOT MET — and the reason is measured. pr-validation.yml produced zero runs because PR #221 is CONFLICTING and GitHub cannot build its merge ref. Locally the same 8-directory loop runs 0 failed / 0 errors (Step 0.7)."
    },
    {
      "claim": "CI ran the frontend suites",
      "command": "the Jest/Vitest job log",
      "before": "never run",
      "target": "real collected counts",
      "result": "NOT MET — same single cause."
    },
    {
      "claim": "The PR gate is green",
      "command": "gh pr checks",
      "before": "unknown",
      "target": "all required checks pass, or the failure captured verbatim",
      "result": "MEASURED AND CAPTURED. Five checks reported, all from security.yml via the push event: auth-enforcement/code-security/iac-security/node-security pass, python-security FAILS on 23 dependency CVEs (verbatim output recorded above). Zero pr-validation checks exist to be green or red."
    },
    {
      "claim": "dev environment gating",
      "command": "gh api .../environments",
      "before": "protection_rules: [] (sweep-00, unverified since)",
      "target": "measured; PASS/FAIL/UNKNOWN",
      "result": "MEASURED — FAIL. `dev` still has protection_rules: []. Not UNKNOWN: the API read succeeded."
    },
    {
      "claim": "Ungated deploy paths closed",
      "command": "re-run the trigger/gate analysis",
      "before": "9 workflows reach make deploy",
      "target": "each gated or explicitly accepted with a reason",
      "result": "MET. 13 deploy paths enumerated. 2 closed by b638b35; 6 already gated; 4 feature-branch workflows explicitly accepted by operator decision; 2 non-existent environments recorded as a new finding."
    },
    {
      "claim": "Handoff 02's suite result holds",
      "command": "uv run pytest -q --tb=no -p no:cacheprovider | tail -1",
      "before": "0 failed / 0 errors",
      "target": "unchanged",
      "result": "MET. Three full runs (one fixed-order, two random-order) all returned 2058 passed, 48 skipped, 12 xfailed."
    }
  ],
  "new_findings_not_in_any_prior_handoff": [
    "PR #221 is CONFLICTING on a single add/add markdown file, and that alone prevents every pull_request-triggered workflow from running. This is the real reason handoff 01's gate has never been observed.",
    "Push-triggered check runs appear on a PR when the head SHA matches, so an empty PR gate can look populated. `gh pr checks` returning five green-ish rows was misleading; `gh run list --event pull_request` was the disambiguating command.",
    "security.yml — the only workflow that runs on a branch push — is RED on 23 dependency CVEs across anyio, cryptography, pypdf and soupsieve.",
    "The `production` and `gap-remediation` GitHub environments do not exist; GitHub would auto-create them with no protection on first use.",
    "The `devx` environment was gated and referenced by nothing, while `dev` was ungated and referenced by six workflows.",
    "tests/infra/test_p28_deploy_identity.py can only assert that an `environment:` is declared, never that it is protected — a structurally half-checkable invariant.",
    "The repository is PUBLIC. The handoff's push-blocker section did not state this, and it materially changes the cost of allowing the secret.",
    "The handoff's rewrite-cost estimate was wrong: the push range is 28 local-only commits (git rev-list --count 51129b1^..HEAD --not --remotes=origin), not 182 commits of shared history."
  ],
  "deviations_from_the_handoff": [
    "Model: ran as Opus 5, not the specified Sonnet 5. Recorded in `model` above.",
    "Step 3's I4_identity writer was redirected alongside the three the handoff named. Same file, same hazard.",
    "Step 2: the handoff suggested required reviewers on `dev` as the cheapest holding fix. The options were costed and the user chose the `devx` repoint instead; the handoff's suggestion was not applied and remains available.",
    "Step 1: the handoff's proof commands assumed a successful push would be sufficient. It was not. The push succeeded and the gate still did not run, so the obligation was re-derived down to the merge-ref mechanism."
  ],
  "carried_forward_not_fixed": {
    "immediate": [
      "PR #221 is OPEN and CONFLICTING. One file: docs/db-redesign/code/code-analysis/project/runbooks/wave-1-status.md. Resolving it is the entire remaining cost of paying handoff 01's proof.",
      "The AWS Access Key ID is now on a PUBLIC remote and is NOT rotated.",
      "security.yml python-security is red on 23 CVEs in 4 packages.",
      "The four feature-branch workflows still deploy to the ungated `dev` environment on push, by operator decision.",
      "`production` and `gap-remediation` environments do not exist and would be created ungated."
    ],
    "from_handoff_02": [
      "The eviction dance may be deletable outright — __editable___service_cdk_1_0_finder already maps bare `careervp` to infra/careervp and the submodules are disjoint. careervp_root() may be unnecessary. Needs its own proof; do not remove casually — it is currently the only thing holding a 0/0 suite.",
      "8 test files mutate os.environ directly instead of via monkeypatch.",
      "careervp/logic/auth_service.py:84 has a never-cleared @lru_cache(maxsize=1).",
      "~28 test files sys.path.insert(0, INFRA_SRC) without restoring."
    ],
    "from_handoff_01": [
      "filterwarnings = ['error'] produces 137 failures, ~126 from aws_lambda_powertools 'No application metrics to publish'. Separately, coroutines-never-awaited warnings for generate_gap_questions and _async_process_record read as real missing-await bugs. Still the strongest candidate for a dedicated session.",
      "vpr_handler.py is dead code; the real path is vpr_submit_handler -> SQS -> vpr_worker_handler. Journey step J5 unmeasured.",
      "CVTailoringRequest.vpr_id (careervp/models/api_models.py:343) has no `= None` default. Quarantined xfail(strict=True).",
      "DynamoDalHandler API mismatch in test_dal_migration_integration.py. Quarantined.",
      "interview_prep_prompt renamed API. Quarantined.",
      "48 unit + 7 integration files call now()/utcnow()/today() with freezegun available but unused."
    ],
    "from_sweep_00": [
      "preflight.py:366 and scripts/ci/changeset_replacement_report.py query AWS::DynamoDB::Table while every table is AWS::DynamoDB::GlobalTable — the deploy gate's DynamoDB auto-fail can never trigger. Load-bearing.",
      "31 of 32 devx Lambdas have 1-day log retention.",
      "careervp/handlers/knowledge_base_handler.py is routed to nothing.",
      "Five jest 'e2e' billing files are pure tautologies; npm run test:e2e deliberately kept out of the PR gate.",
      "CLAUDE.md's naming-check command uses `python`, which does not exist on this machine (python3 / uv run python only)."
    ]
  },
  "what_success_looked_like_vs_what_happened": {
    "predicted": "Clear push protection, push, open a PR, read 8 ::group:: markers and real Jest/Vitest counts out of a green pr-validation run.",
    "happened": "Push protection was cleared and the push succeeded. The PR opened. pr-validation.yml did not run at all — not red, not green, absent — because a single conflicting markdown file prevents GitHub from building the PR's merge ref. The only workflow that did run, security.yml, was red on dependency CVEs that no prior handoff had recorded.",
    "the_gap_is_the_finding": "The plan's rule says a prediction that misses means the model of the system was wrong. It was: 'the CI gate is unverified' was treated as 'nobody has pushed yet'. It was actually 'the gate cannot fire on this PR'. Those need different fixes, and only reading the run distinguished them.",
    "honest_scope": "No product code was touched. Three commits, each one concern. The backend suite is unchanged at 2118 tests, 0 failed / 0 errors. Two of the three deferred items from this handoff are fully paid (W5 gating, evidence churn); the CI observation is paid down from 'blocked, cause unknown' to 'blocked, cause measured and one file wide'."
  }
}
