# Wave 3 — Status Ledger

**Read this file FIRST before starting any Wave-3 prompt.** `wave-3-prompts.md` describes what
*should* happen; this file describes what *actually* happened, and is what every prompt checks
before starting its own work (see `RUNBOOK-RULES.md`, rules 2–3). Update your own row when you
finish a step or stop on a problem — do not leave this file stale for the next session to trip
over.

Rows are listed in dependency order. Before starting a step, read the row above it (or the rows
it depends on per `wave-3-prompts.md` §2) — if any of them show an open problem, resolve that
first.

---

## ✅ AUTHORIZED — Wave 2 GATE passed

**This ledger and `wave-3-prompts.md` were authored on 2026-07-26 *ahead of* the Wave-2 GATE, by an
explicit human decision to prepare Wave 3 in parallel (authoring is gate-safe: no code, no test, no
deploy crosses the barrier).** The barrier is now clear: the `GATE` row in
[`/Users/yitzchak.meirovich/Documents/code5/careervp/docs/db-redesign/code/code-analysis/project/runbooks/wave-2-status.md`](./wave-2-status.md)
reads **PASSED** from evidence `docs/evidence/wave2-gate-20260726T205022Z-d8707a.json`
(9 PASS, 0 FAIL, 0 HUMAN_REQUIRED, 2 RECORDED), and the required smoke harness evidence
`docs/evidence/smoke-20260726T205022Z-fdac58.json` is 4/4 green. Wave 3 may now execute, starting
with 3.1-RED.

---

**Deploy target: `CareerVpCrudDevx`** (project-wide since 2026-07-25 — `CareerVpCrudDev` is being
retired). Anything pointed at `api.dev.careervp.com` is talking to the OLD stack; use the raw invoke
URL `https://ymzhvcxod0.execute-api.us-east-1.amazonaws.com/prod/` until the human-only base-path
flip happens. **No Wave-3 work merges to `main`** — the push-to-`main` CI path still targets the old
stack (Wave-2 bet `B-2-4`, item 1, still open).

> **⚠️ CHANGED 2026-07-31 — on `db-redesign`, A SYNC IS NOW A DEPLOY.** `db-redesign-checks.yml`'s
> `deploy-backend-dev` job fires on every push touching `src/backend/**` or `infra/**` and, since
> 2026-07-31, deploys **`CareerVpCrudDevx`** via `make deploy-devx` (`ENVIRONMENT=devx`). Before
> 2026-07-31 it deployed **`CareerVpCrudDev`** — the retired stack — which is why steps 3.2-GREEN and
> 3.3-RED/GREEN recorded "undeployed debt" against devx while CI was reporting successful deploys.
> Both statements were true at once; see the corrected item 3 in the 3.3-GREEN row.
> **Practical consequence: do not push an infra change until its isolated template diff is in hand.**
> **Still open:** this path calls `make deploy` (create **and** execute, `--require-approval=never`),
> which the Makefile's own P-28 comment forbids from automation; `deploy.yml`'s change-set +
> human-gated-execute shape is the compliant pattern and has not been adopted here.

| Step | Clause(s) | Status (plain English) | Open problem for the next step | Commit | Date |
|---|---|---|---|---|---|
| 3.1-RED | D-H2, D-H3 | **RED LANDED in the working tree after the human D-H2 contract correction.** Added exactly four tests in `src/backend/tests/unit/test_dh2_dh3_key_authority.py`; all four collect and fail on their intended AC assertions. The artifact-key scan reports **56** live artifacts/core key-building sites while explicitly excluding auth/trial/user-pool keying; its B-3-5 `USER#` ratchet re-confirms **9 sites / 5 files**. The D-H3 and canonical-only tests catch the absent module import inside the test and fail via explicit `AC-DH3-1` / `AC-DH2-1` messages naming `careervp.dal.core_repository`, not collection errors or skips. The env-precedence scan reports the spec's full enumerated **22 locations / 9 files** and records that the heading says 23 although the list sums to 22; the test retains 23 as the maximum and freezes all 22 source signatures so a new site fails. B-3-2 is confirmed TRUE/default-on in `ISSUES.md` (`dynamo_dal_handler.py:628-637`, flag default `'true'` at `:621`; scan twin at `:677-683`); B-3-5's count delta is recorded there. Verification: repository-wide Ruff format/check clean; strict mypy clean on the new file; focused existing regressions `18 passed`; pre-existing unit suite with only this intentional RED file excluded `1374 passed, 15 skipped, 4 xfailed, 1 xpassed`; `check_scope_lock_integrity.py --base HEAD` reports `OK`. Zero files under `src/backend/careervp/` were modified. Prompt and corrected D-H2/D-H3 YAML clauses match; no harness test or AC-DH2-2 was added. | **3.1-GREEN must run in a fresh session and may not edit this RED file.** Create importable `careervp.dal.core_repository.CoreRepository` and `careervp.dal.table_registry.TableRegistry`; re-home the 56 reported artifacts/core key constructions; remove the 22 reported handler precedence sites without widening either ratchet; preserve D-H3's three outcomes (retry-hit succeeds with item, retry-miss becomes `TABLE_SCHEMA_MISMATCH`, retry-raises unchanged); make every canonical cover-letter read return the canonical-only moto item. **Human commit ceremony remains:** the correction note says to add the v2.7.1 §12 row to the MD twin and use a `Scope-Lock-Approved-By:` trailer. The current integrity command returns `OK`, so it does not enforce that missing row in the working tree; do not mistake that tooling result for the human approval trailer a commit still needs. | pending — proposed commit message: `test: add RED key-authority and schema-mismatch contracts` | 2026-07-27 |
| 3.1-GREEN | D-H2, D-H3 | **GREEN — all four RED tests pass with implementation-only changes; zero test files and zero spec RED-briefs modified** (the only test change is a NEW 9-test characterization file, `src/backend/tests/unit/test_table_registry_characterization.py`, pinning key grammar and env-resolution semantics before re-homing). Created `careervp/dal/table_registry.py` (sole artifacts/core key-grammar + table-name-resolution authority) and `careervp/dal/core_repository.py` (repository entry point; canonical cover-letter read `CoreRepository.get_cover_letter_by_artifact_id`). All **56** reported artifacts/core key sites re-homed out of `handlers/`+`logic/` and all **22** enumerated env-precedence sites removed from the 9 handler files — both scans now assert zero live sites; neither ratchet baseline was widened, and the full baseline cleared in one session (no B-3-5 residue enumeration needed). The env chains survive **only inside the registry**, deliberately as three distinct named roles (full artifacts chain, legacy two-key tail, company-research chain) because `ai_assist_nested_stack.py` points `ARTIFACTS_TABLE_NAME` and `COMPANY_RESEARCH_TABLE_NAME` at **different physical tables** — collapsing them would silently retarget reads. `dynamo_dal_handler.py`'s `save_cv` legacy-alias keys and key-prefix constants now come from the registry. D-H3: both named swallow sites (`read_cover_letter_by_artifact_id` get_item retry and the `_legacy_read_cover_letter_by_scan` twin) return `Result(success=False, code=TABLE_SCHEMA_MISMATCH)` with an error log on a ValidationException retry-miss; retry-hit and retry-raise outcomes unchanged, per the spec's three-outcome GREEN boundary. Verification, all fresh: 4 RED tests pass; full suite `1553 passed, 30 skipped, 4 xfailed, 1 xpassed`, zero regressions; Ruff format+check clean; `mypy careervp --strict` clean (136 files); `make coverage-tests` exit 0 — overall 73.13/54.50, core 73.36/**55.52** (Wave-2 measured 55.49 — core-branch ratchet held and improved), supporting 73.67/52.26; oracle tests (`test_frontend_oracle_schema_emission.py` + `test_route_parity_openapi.py`) 5 passed — no §3 identifier/response-shape drift; `check_scope_lock_integrity.py --base HEAD` OK. Also verified: the RED commit `fb41d22` landed **with** the human `Scope-Lock-Approved-By` trailer, so the ceremony flagged in the 3.1-RED row is complete. | **Parity-harness import path: none — the harness was retired at scope-lock v2.7.0; nothing for 3.2/3.4/3.5 to import.** They extend `careervp.dal.core_repository.CoreRepository` / `careervp.dal.table_registry` instead (contention hotspot — serialize per §2). Residues recorded, not hidden: (a) `logic/company_research_store.py::_legacy_table_name` still resolves `TABLE_NAME → DYNAMODB_TABLE_NAME` (reversed legacy order) — it is outside the handler-scoped baseline and dies with the CR legacy path in 3.5 (D-H9 demolition); (b) `dal/dynamo_dal_handler.py` still builds keys internally — `dal/` is outside the scan boundary and its grammar constants now come from the registry, but collapsing its key building into `CoreRepository` is later-wave work; (c) the inner query-level fallback in `_legacy_read_cover_letter_by_scan` (canonical-query ValidationException → legacy query → miss at the `allow_sk_match=False` match) still returns success-None — the spec's GREEN boundary named only the two outer sites; flag for 3.5 when the whole legacy scan path is demolished. | `f438355` | 2026-07-27 |
| 3.2-SPEC | D-H4, P-01 | **SPEC PINNED — four RED descriptions now state exact values, the three ACs are unchanged, and no v2.7.0 compatibility machinery returned.** Live A–G citations were re-read and all deltas recorded in the spec. The six-name resolver ladder is pinned to `artifact_id` only; cover letter and interview prep share one exact HTTP 403 flat denial envelope; interview-prep ownership refusal is terminal; `interview_prep_submit_handler.py:144` is explicitly in scope so `vpr_id` cannot become `application_id`; the three 3.1 residues remain out of scope. `B-3-6` settled **TRUE at the handler boundary**: the Pydantic model is correct, but `cv_tailoring_handler.py:600-607` lets omitted `vpr_id` bypass validation and the probe returned the same stubbed 202 as present-null. `B-3-7` settled **FALSE**: a moto probe forced the second application update to fail and observed an existing canonical artifact with hub `artifact_id: null`; `application_repository.py` is therefore pinned and the failure must surface as `DYNAMODB_ERROR`. Only the spec, `ISSUES.md`, and this pre-existing ledger change were edited; zero source and test files changed. | **No bet remains undecided for 3.2-RED:** write the four tests exactly as pinned; B-3-6's handler half is behavior-changing RED, not a regression guard, and B-3-7 requires the forced write-failure stimulus. Human-review flag before 3.2-GREEN closes anything: D-H4 AC-DH4-2 owns the observable distinction in Wave 3, while F-04 remains a Wave-4 `TARGET/live_bug`; do not mark F-04 closed. | pending — proposed: `docs(db): pin 3.2 canonical artifact contracts and bets` | 2026-07-27 |
| 3.2-RED | D-H4, P-01 | **RED LANDED in the working tree — exactly four tests collect and fail on their intended D-H4/P-01 assertions; no implementation or frontend files changed.** Rule 14 passed: the spec exists, names AC-DH4-1/AC-P01-1/AC-DH4-2 with exact values, and `grep -w or` returned no match in the RED section. The hub test uses moto, forces the second application artifact-id update to raise, and observes canonical `cl-001` existing while the hub emits `artifact_id: null`. Both live generation handlers run stale and cross-tenant stimuli and prove they call `dal.get_vpr` with the client keys instead of returning the shared flat 403 denial. The CV-tailoring model half is already green, while the handler still returns 202 for omitted `vpr_id`; B-3-6 is therefore behavior-changing RED, not a guard, and the F-04 overlap remains flagged for human review. Missing APIs are caught inside tests with explicit AC-owned failures rather than collection errors or skips. Verification: four intentional failures, strict mypy clean, repository-wide Ruff format/check clean, and the unchanged D-H2/D-H3 ratchets remain 4 passed. Prompt scope and the D-H4/P-01 YAML clauses match exactly. | **3.2-GREEN must not edit the RED file.** First resolve canonical hub identity and return `Result(success=False, data=None, code=DYNAMODB_ERROR)` for the forced step-2 write failure; remove the five alias names from generic artifact-id resolution; resolve VPRs by authenticated user/application before reads; return the identical pinned 403 envelope for cover letter and interview prep; make interview ownership refusal terminal; preserve submit routing as `application_id == job_id` and reject both absent; and reject omitted CV-tailoring `vpr_id` while accepting present-null. Create `resolve_artifact_id`, `CoreRepository.get_vpr_by_artifact_id`, and `CoreRepository.get_interview_prep_by_artifact_id`. Do not close Wave-4 F-04. | pending — proposed commit message: `test: add RED canonical artifact routing contracts` | 2026-07-27 |
| 3.2-GREEN | D-H4, P-01 | **GREEN — all four pinned RED tests pass; full backend suite 1561 passed / 0 failed.** Both locked contract conflicts were settled by human decision BEFORE any implementation landed, each as its own visible commit. **B1** (CV-tailoring omission): the fixture was stale, not the contract — scope-lock §3 item 3 (`vpr_id: null`, never omitted), `contract/schemas/CVTailoringRequest.json` (`required: [cv_id, job_id, vpr_id]`), `types.ts:517` and the only live caller all already agreed; the fixture passed solely via the handler bypass `B-3-6` settled TRUE. No contract change. **A1** (interview-prep identity): the public request contract is TIGHTENED — `application_id` or `job_id` is required and the `application_id or job_id or vpr_id` fallback at `interview_prep_submit_handler.py:144` is REMOVED, because a client vpr_id standing in for the canonical application key is legacy-id resolution (O-3) and fed `artifact_dependency_utils.py:183-195`, which wrote `update_artifact_status(application_id=<a vpr_id>)` into the canonical hub behind a blanket `except Exception: return` — a silent bad write in the same step D-H4 hardens bad writes. Missing identity is now refused HTTP 400 BEFORE dependency resolution: no resolver read, no pending write. Recorded as scope-lock **v3.0.0 MAJOR** (twin-synced, §12 row, approval trailer). **Verification (fresh):** 4 pinned pass · dh2/dh3 ratchets 4 pass · full suite 1561 passed 0 failed · `mypy --strict` clean · ruff clean with **no new suppression** (the guard tripped C901 11>10, so both preconditions moved into `_resolve_submit_preconditions` rather than adding a `noqa`) · `make coverage-tests` **exit 0**, core branch **55.79%** vs the 3.1 ratchet 55.52%, overall 73.33/54.76 · oracle + route-parity 5 pass · frontend typecheck clean + 149 unit tests · scope-lock integrity OK. Oracle §3 items 1, 2 and 3 hold; **no route versioning added**. Test edits were human-approved reconciliations, not weakened assertions: no pinned RED test and no spec RED-brief was modified. | **1. The §0.3/§9.2 ADVERSARIAL REVIEW for the v3.0.0 contract amendment is NOT discharged** — required before a contract-surface amendment is final; a human owes it. **2. UNDEPLOYED DEBT (§0.5):** nothing was deployed to `CareerVpCrudDevx` and no raw-invoke-URL characterization was run. D-H4 verifies `contract+integration` and P-01 verifies `e2e+characterization`, so the unit/integration suite does NOT discharge either clause. This is Wave-3's first undeployed step — do not let it accumulate as Wave 2 did. **3. The §0.3 proposal inventoried 1 of 8 affected tests**; the other 7 were found only by running the full suite (1 in `test_async_submit_handlers.py`, 5 fed by the shared `_valid_request_body()` helper in `test_interview_prep_roundtrip.py`). Full list is now in the spec's affected-existing-tests inventory. Future RED briefs must carry that inventory. **4. P-28 gate quirk found:** `check_scope_lock_integrity.py` reads only `git log -1 --format=%B <head>`, so a twin change in a mid-branch commit FAILS at merge unless the branch TIP also carries the `Scope-Lock-Approved-By:` trailer. The tip commit repeats it. **5. Pre-existing, not introduced here:** both twins were at `2.7.1` with no `2.7.1` change-log row. **6. Repository surface 3.3/3.4/3.5 extend** (added by 3.2, unchanged by this row): `CoreRepository(table_registry, dal, vpr_jobs_repository)`, `resolve_artifact_id(application_id, artifact_type, *, user_id)`, `get_vpr_by_artifact_id(...)`, `get_interview_prep_by_artifact_id(...)`; `TableRegistry.canonical_application_condition(application_id)`, `TableRegistry.canonical_artifact_id(item)`. The generic resolver accepts ONLY exact `artifact_id`. **7. Residue unchanged, named owners:** `company_research_store::_legacy_table_name`, DAL-internal key building and `_legacy_read_cover_letter_by_scan` → 3.5/later; `infra/` and the D-M split → 3.4; request-path Scans → 3.3; auth/trial keying → Wave 6; F-04 → Wave 4; P-07b/I-05/I-06 untouched. | `0ef8971` B1 · `7807e43` v3.0.0 twins · `ae18e30` contract+tests · `a485293` impl | 2026-07-27 |
| 3.3 | D-H7 | **PROMPTS FILLED IN 2026-07-29 — not started.** `wave-3-prompts.md` §3.3 now carries three full prompts (3.3-SPEC → 3.3-RED → 3.3-GREEN), replacing the skeleton. Three findings from the fill-in pre-flight, all docs-only, zero source/test/infra files touched: (1) **`B-3-8` added** to `ISSUES.md` and pre-settled toward FALSE — all three live `.scan(` sites are already owned by prior decisions (`subscription_repository.py:415` kept by Wave-2 2.1-GREEN for `BillingReconcileLambda`; `dynamo_dal_handler.py:800` owned by 3.5 as 3.1-GREEN residue (c); `scripts/cr_migration_backfill.py:261` offline and allow-listed), so D-H7 is most likely a **guard-rail step**, not a removal. (2) **The D-H7 spec is stale** — its Evidence cites `subscription_repository.py:127-129` as a surviving money-path scan; live, that function already queries `customer-id-index` and Wave-2 2.1-GREEN removed that scan. Rule 14 forbids RED from pinning its own spec, hence the separate 3.3-SPEC step (3.2's precedent). (3) **Two new live findings the spec never mentions:** `infra/careervp/api_construct.py:941` still grants `dynamodb:Scan` on the artifacts table, and CDK's ~20 `grant_*_data` calls grant Scan implicitly — so AC-DH7-1's "Scan is never called" is ambiguous between source and IAM. Also confirmed **good news**: AC-DH7-2 is already satisfied — `status-index` (`api_db_construct.py:384-393`) has partition key `userId` with `status` only in the sort position; the name is a red herring. | **3.3-SPEC runs first and must settle three things before any test is written:** `B-3-8` (tier-1 inventory, classify every hit against an EXISTING decision, never against the spec); **DP-1** — does 3.3 touch `CoreRepository`/`TableRegistry` (Option A no-touch / B new-write / C read-only), which decides whether 3.3 holds the §2 serialization lock at all; **DP-2** — does AC-DH7-1 mean source-only or IAM, recommended "source + the one explicit grant at `api_construct.py:941`", with a rule-10 STOP that hands the ~20 implicit grants to 3.4 rather than reshaping `api_construct.py` concurrently with it. Both DPs are defined with options, resolving evidence, and stopping conditions in `wave-3-prompts.md` §3.3 — **deferred deliberately, and whoever resolves one owes a plain-English explanation in their row.** Standing warning carried into RED and GREEN: `dynamo_dal_handler.py:800` is the site that would make 3.3 feel substantial and it is **3.5's** — annexing it is a rule-5 stop. | — (docs only) | 2026-07-29 |
| 3.3-SPEC | D-H7 | **SPEC PINNED — the three RED descriptions now state exact values, the two ACs are unchanged, and 3.3 turned out to be a smaller step than the spec's title implied. That is the finding, not a problem.** In plain English: nobody's code is secretly scanning the whole database on a user request, so there is nothing to speed up — the job is to lock the door so it cannot start happening again, plus remove one over-broad AWS permission. `B-3-8` settled **FALSE**: all three places in the code that scan a whole table are already spoken for by earlier decisions — the billing-reconciliation scan Wave 2 chose to keep, the old cover-letter scan that belongs to step 3.5, and an offline script that gets deleted at 3.5 — so **not one of them is 3.3's to remove**. **DP-1 took Option A:** 3.3 does not touch the shared `CoreRepository`/`TableRegistry` code at all, so it needs no exclusive lock and **can run at the same time as 3.4 and 3.5**. **DP-2 took "source + the one explicit grant":** besides the automated check over the application code, 3.3 deletes one line of permission config (`infra/careervp/api_construct.py:941`) that lets a function scan the artifacts table even though no code ever asks to — one line, provable by a build-time assertion, so **3.3 holds the `infra/` lock for that single edit** and `B-3-4`'s isolated-template-diff technique applies. The larger permission cleanup (**22** implicit CDK grants, live count — the pre-flight's "~20" was an estimate) is handed to **3.4**, which is already reshaping that file; DP-2's rule-10 STOP is therefore **not triggered**, because it fires only for the full-IAM option, which was not taken. Findings recorded live: the spec's Evidence pointed at a money-path scan that no longer exists (those lines are now a section comment); the real pre-existing duplicate of RED test 2 is `test_p14_p15_billing_idempotency.py:198-220` (AC-P15-1), not the test Wave-0 cited, so that test is labelled a duplicate-by-design guard whose value is AC ownership rather than new coverage; and two vacuous-pass traps are pinned (the IAM policy lives in the **features** nested-stack template, not the parent, and tables synthesize as `AWS::DynamoDB::GlobalTable` while `AWS::DynamoDB::Table` counts **0**). AC-DH7-2 confirmed **already satisfied** — `status-index` is partitioned by `userId` with `status` only as the sort key. Foundation and baseline verified before editing: `CoreRepository` + `TableRegistry` both import from the interpreter, and `tests/unit` is **1395 passed, 15 skipped, 4 xfailed, 1 xpassed, 0 failed**. Only the spec, `ISSUES.md`, and this row were edited; **zero files under `src/` and `infra/` changed** (`git status --porcelain src/ infra/` empty). | **Nothing is left undecided for 3.3-RED — write the three tests exactly as pinned.** Note that RED test 1 has two parts: Part A (the static source guard) is green on day one and must be **labelled a guard**; Part B (removing the `dynamodb:Scan` grant) is the **only** assertion in the whole step that is red first and green after. Part A must be a **frozen-baseline ratchet with exactly two enumerated allow-listed sites**, never an absolute zero-occurrences assertion — an absolute assertion cannot pass without either annexing 3.5's site or dropping `dal/` from scope, which are a rule-5 stop and a false green respectively; the ratchet must still pass unedited when 3.5 later deletes its site. RED test 2 must **not** assert anything about `scan_active_subscriptions`, which Wave 2 deliberately kept — an over-broad assertion there breaks the reconcile path. RED test 3 must use `AWS::DynamoDB::GlobalTable` and assert the resource set is non-empty first. **Standing warning still in force:** `dynamo_dal_handler.py:800` is the one site that would make 3.3 feel substantial and it is **3.5's** — annexing it is a rule-5 stop. **FLAGGED FOR HUMAN REVIEW, not fixed here and not assigned an owner:** `careervp/handlers/artifact_cleanup_handler.py:188` calls `jobs_repo.scan_by_status(...)`, a method that **does not exist anywhere**; because `deps` is typed `Any` mypy cannot see it and the resulting error is swallowed, so the scheduled orphan-cleanup job silently cleans nothing on every run. It is **not** a fourth Scan (no Scan is ever issued, and it is a scheduled reaper rather than a request path), so it does not flip `B-3-8` — but it is a real latent bug that belongs to a human, not to 3.3. **Also carried forward:** scope-lock D-H7 is `verification: integration`, so like 3.2 a passing unit/synth suite does **not** discharge the clause — 3.3-GREEN inherits Wave-3's undeployed-debt problem against `CareerVpCrudDevx` and must record it rather than claim closure. **Minor staleness left deliberately in bounds:** the `B-3-8` row in the bets index below still reads "PRE-SETTLED TOWARD FALSE"; `ISSUES.md` is the authoritative text and now reads **SETTLED FALSE**, and this session was scoped to edit only the spec, `ISSUES.md`, and this row. | `f403e68` · `d9479ff` (follow-up: or-gate, GSI conflict, IAM breadth) | 2026-07-29 |
| 3.3-RED | D-H7 | **RED LANDED in the working tree — three test names, four pytest node ids, exactly one intended failure and three labelled day-one guards; zero implementation files touched.** In plain English: the automated door-lock is in place and already holds, and the one real change 3.3 makes — deleting an over-broad AWS permission — now has a test that fails until someone deletes it. Rule 14 passed before any test was written: the spec exists (35 769 bytes), names AC-DH7-1/AC-DH7-2, states exact assertion values, and its allow-list is **enumerated site by site (exactly two entries)**, not a directory wildcard. The `or`-shape gate was checked on **assertion values, not the bare word**: 10 bare-word `or` hits exist in the pinned RED section (lines 239-510) and **every one is prose** — "added, removed, or renamed", "annexing 3.5's site or dropping `dal/` from scope", "a variable, or `eval`/`exec`", and two hits that are the spec *quoting options it explicitly REJECTS* ("the repository never scans, or …", "every PK is user-scoped or high-cardinality"). No assertion accepts either of two outcomes. (The 3.3-SPEC note predicted "seven prose hits"; the live count over the pinned section is **10** — a counting delta in prose, recorded rather than adopted silently, and not a stopping condition.) Files, both under `tests/` and both new: `src/backend/tests/unit/test_dh7_request_path_scans.py` (Part A + the subscription guard) and `src/backend/tests/infrastructure/test_dh7_scan_iam_and_gsi_shape.py` (Part B + the GSI guard). **`src/backend` was chosen as the single home for the whole D-H7 suite** so one selector reaches all of it; both module docstrings state that `pytest tests/unit -k dh7` alone is a **false green**, and the honest selector is `uv run pytest tests/unit tests/infrastructure -q -k "dh7"`. **RED (1):** Part B of `test_dh7_no_scan_in_runtime_handlers_or_dal` — the inline `artifacts_table` policy still lists `dynamodb:Scan` alongside the five actions it needs. Scoped to that inline statement **only**; it is deliberately NOT unioned with the role's attached `ServiceRoleArnDefaultPolicy2B096FD3`, where Scan is also present from 3.4's 22 implicit grants and which would make the assertion unsatisfiable by 3.3. **GUARDS (3), each labelled in its docstring with the reason and each PROVEN ABLE TO FAIL without editing one implementation file:** Part A's AST detector reported all four poisoned forms (`table.scan(...)`, `fn = table.scan`, `getattr(table,'scan')(...)`, `get_paginator('scan')`) from a throwaway file in `tmp_path`, and returned **zero** sites for the two live regex false-positive traps (`ScanIndexForward=False`, a `'list-scan fallback'` log string); the GSI guard failed on a hand-built template with `HASH=status` and again on a 9th index breaking the frozen 8-entry set equality; the subscription guard failed with `Expected 'scan' to not have been called. Called 1 times.` once the double routed `query` onto `scan`. Every detector and assertion helper takes its input as a **parameter**, so the proofs needed no mutation to revert — `git status --porcelain src/backend/careervp/ src/frontend/ infra/careervp/` is **empty**. Part A's ratchet is `found ⊆ BASELINE` (shrink allowed, growth forbidden) over 105 live files (handlers 43, dal 17, logic 45), with baseline resolvability asserted at **file** granularity so 3.5 deleting its site is a clean shrink rather than a build break. Verification, all fresh: D-H7 suite both roots **1 failed / 3 passed**; 3.1 ratchets + 3.2 contracts **8 passed, unchanged**; P-15 precedents (`test_p14_p15_billing_idempotency.py` + `test_p15_billing_iam.py`) **5 passed**; `tests/unit` **1397 passed, 15 skipped, 4 xfailed, 1 xpassed, 0 failed** (1395 baseline + the 2 new unit tests); `tests/infrastructure` **75 passed, 1 skipped, 1 failed** — the intended RED and nothing else; ruff format + `check --fix` clean repo-wide; `mypy --strict` clean on both new files and on `careervp` (136 files); `check_scope_lock_integrity.py --base HEAD` **OK**. | **1. FLAGGED FOR HUMAN REVIEW — a pinned spec value is factually wrong and 3.3-RED corrected it rather than shipping a deadlock.** Plain English: the spec's allow-list says the old cover-letter scan lives in a function called `_legacy_read_cover_letter_by_scan`, but that function does not scan anything; the real scan is a few lines away in a differently-named function, so a test written from the spec word-for-word would have failed on day one and could only have been made to pass by deleting code that belongs to step 3.5. Technically: the spec's allow-list row 2 keys the site as `dynamo_dal_handler.py :: _legacy_read_cover_letter_by_scan`, but live that function (`:663-712`) issues **no** `.scan(` at all and the real Scan at `:800` has enclosing def `legacy_read_cover_letter` (`:782`). Since the spec keys the baseline by `(path, enclosing_function_name)`, the literal key would leave the live site unlisted → guard red on day one → satisfiable only by annexing 3.5's site (rule-5 stop) or by 3.3-GREEN editing this RED test (forbidden). The baseline therefore uses the live name, which is also the name `ISSUES.md` `B-3-8` already records; the site itself is unambiguous (same file, same line `:800`, same verbatim scan expression). The correction is documented inline in `SCAN_BASELINE`. **A human should confirm this reading and fix the spec's E-1 table and allow-list row 2.** **2. Also flagged — a prompt-internal conflict, resolved toward the spec's stated intent.** The prompt says Part A and Part B are "TWO PARTS, ONE TEST — do not split it", and *also* pins Part A to the unit file and Part B to the synth file. Both cannot hold literally. Resolution: **both parts carry the identical test name** `test_dh7_no_scan_in_runtime_handlers_or_dal` in their respective modules, so the step has exactly **three test names** and Part B has no identity of its own — which is what the spec's own wording protects ("the IAM assertion is pinned as Part B of test 1 rather than as a new test — a fourth RED test is out of bounds"). The cost is two pytest node ids sharing one name; a human should confirm this over the alternative of one function in one file. **3. A spec-permitted companion assertion was deliberately NOT written**, and this is a judgment call a human may reverse: the spec "permits and encourages" asserting that `dynamodb:Scan` **is still present** in the attached default policy, labelled 3.4's residue. Written as a hard assertion it inverts into a landmine — when 3.4 narrows the 22 grants, a D-H7 test fails and 3.4 can only clear it by editing a D-H7 test. The fact is recorded in the helper's docstring with its owner and the live 2026-07-30 verification instead, so the 22-grant surface stays proven-and-owned without planting that trap. **4. 3.3-GREEN must not edit either RED file.** It creates **no new symbols** — DP-1 took Option A, so nothing is added to `CoreRepository`/`TableRegistry` and there is no §2 serialization lock to hold. Its entire job is to delete the literal `'dynamodb:Scan'` at `infra/careervp/api_construct.py:941`, leaving the action list exactly `[PutItem, GetItem, UpdateItem, DeleteItem, Query]` in that order and the two `Resource` entries (table ARN + `type-index`) untouched. It owes the **`B-3-4` isolated-template-diff proof** (HEAD vs change-stashed, zero replacement markers on stateful resources, the only expected difference being that single `Action` element) and it holds the `infra/` lock for that one edit — `api_construct.py` may not be edited by 3.4 concurrently. **5. UNDEPLOYED DEBT (§0.5) carried forward, not discharged:** scope-lock D-H7 is `verification: integration`, so a green unit+synth suite does **not** close the clause. Nothing was deployed to `CareerVpCrudDevx`. This is Wave-3's second undeployed step — do not let it accumulate as Wave 2 did. **6. Still open from 3.3-SPEC, unchanged and not mine:** `handlers/artifact_cleanup_handler.py:188` calls the non-existent `jobs_repo.scan_by_status(...)`, so the scheduled orphan sweep silently reaps nothing on every run — flagged for a human, no owner asserted; it is correctly NOT flagged by Part A's guard (`attr != 'scan'`, and no Scan is ever issued), and that known limit is named in the detector's docstring. **7. Residue unchanged, named owners:** `dynamo_dal_handler.py:800` → 3.5; `subscription_repository.py:415` retained by Wave-2 `2.1-GREEN`; `scripts/cr_migration_backfill.py:261` offline, deleted at 3.5; the 22 implicit grants → 3.4; `entity-index`'s `knowledgeType` partition key → D-M5 (3.4) and Q-07 (Wave 4); auth/trial keying → Wave 6. | `df6860c` | 2026-07-30 |
| 3.3-GREEN | D-H7 | **GREEN — the D-H7 suite is 4/4 green across BOTH pytest roots, from a ONE-LINE implementation change; zero test files and zero spec RED-briefs modified.** In plain English: one AWS permission let a function scan the whole artifacts table even though no code ever asks to. That permission line is now deleted, and a build-time test proves it stays deleted. Nothing else changed — no application code was touched at all, because the door-lock guard was already holding. **The mix RED predicted was confirmed before any edit:** `1 failed, 3 passed` over `tests/unit tests/infrastructure -k dh7`, the single failure being Part B of `test_dh7_no_scan_in_runtime_handlers_or_dal` in `tests/infrastructure/`; the three day-one guards passed. `infra/tests -k dh7` collects **0** tests (154 deselected) — the whole suite lives under `src/backend`, so the honest selector is `uv run pytest tests/unit tests/infrastructure -q -k "dh7"` and `tests/unit` alone (2 passed) is the documented false green. **The fix:** removed the literal `"dynamodb:Scan"` at `infra/careervp/api_construct.py:941`. The inline `artifacts_table` action list is now exactly `[PutItem, GetItem, UpdateItem, DeleteItem, Query]` (order included, asserted) with both `Resource` entries — the table ARN and its `type-index` — intact. `grep -rn '"dynamodb:Scan"' infra/careervp/` now returns **nothing**. **`B-3-4` isolated-template-diff proof discharged** (HEAD vs change-stashed, `ENVIRONMENT=devx cdk synth CareerVpCrudDevx`, no live stack): the entire textual diff is **two lines** — `6570d6569 < - dynamodb:Scan` — and nothing else. Structurally: 491 logical ids before and after with **identical id sets**, exactly **one** changed resource, `CareerVpCrudDevCrudServiceRoleArn305AAC1B` of type `AWS::IAM::Role`; `Parameters`/`Outputs`/`Conditions`/`Mappings`/`Rules`/`Transform` all byte-identical. **Zero stateful replacement:** all 11 `AWS::DynamoDB::GlobalTable`, all 6 `AWS::S3::Bucket` and all 5 `AWS::CloudFormation::Stack` resources byte-stable (the nested-stack byte-stability also proves the nested templates' asset hashes are unchanged, so no nested template moved). **Both IMMUTABLE laws hold, byte-stable:** `AWS::ApiGateway::RestApi CareerVpCrudDevxCrudservicerestapi2258A47A` and `AWS::Cognito::UserPool CareerVpCrudDevxCognitoUserPoolD64DCEC6` (plus the pool client and domain). **Verification, all fresh:** D-H7 both roots **4 passed**; 3.1 ratchets + 3.2 contracts (`dh2 or dh3 or dh4 or p01`) **8 passed, unchanged**; `test_l1_list_endpoints.py` **17 passed**; `test_p14_p15_billing_idempotency.py` (AC-P15-1 money-path scan) **4 passed**; full backend suite `tests/unit/ tests/integration/ tests/infrastructure/` **1639 passed, 31 skipped, 4 xfailed, 1 xpassed, 0 failed**; ruff format (413 files unchanged) + `check --fix` clean with **no new suppression**; `mypy careervp --strict` clean (136 files); `make coverage-tests` **exit 0** — overall 73.33/54.76, core 73.61/**55.79**, supporting 73.90/52.60 (core-branch ratchet **held exactly** at 3.2-GREEN's measured 55.79); oracle + route parity **5 passed**; `scripts/ci/check_scope_lock_integrity.py --base HEAD` **OK**; `infra` suite **154 passed**; `validate_naming.py --path infra --strict` exit 0. Oracle §3 items 1, 2 and 3 hold — replacing a permission grant changes no identifier and no response shape, and **no route versioning was added**. | **1. DP-1 = Option A, CONFIRMED BY DIFF: `CoreRepository` / `TableRegistry` were NOT touched. 3.3 added ZERO symbols to either module** — `git diff --stat` over the whole tree is one file, `infra/careervp/api_construct.py | 1 -`. The §2 serialization hotspot is **untouched and unlocked** for 3.4 and 3.5; the surface they extend is exactly what 3.2-GREEN's row item 6 listed, unchanged. **2. DP-2 = the `infra/` lock WAS taken, and is now RELEASED** — `api_construct.py` was edited for that single line only and 3.4 may proceed on that file. **3. DEPLOY — this row's original claim was WRONG and is corrected here (2026-07-31, human-prompted).** What it first said: *"nothing was deployed"*. **What is actually true:** `db-redesign-checks.yml` has a job `deploy-backend-dev` ("Deploy backend → dev") that fires on **every push to `db-redesign`** touching `src/backend/**` or `infra/**`, gated only on `[ruff-lint, mypy, pytest-unit]`, and it ran `make deploy` → `npx cdk deploy CareerVpCrudDev --require-approval=never`. So Wave-3 code **has** been deploying continuously and automatically — to **`CareerVpCrudDev`, the stack the project is retiring**, not to `CareerVpCrudDevx`. Confirmed live 2026-07-30: `CareerVpCrudDevx` last updated **2026-07-26T19:17Z** (i.e. before 3.2-GREEN landed), while `CareerVpCrudDev` went `UPDATE_IN_PROGRESS` at **2026-07-30T10:26Z** carrying this row's IAM change. **The right framing is not "undeployed" but "deployed to the wrong stack, never verified against the right one."** D-H7 is `verification: integration`, so the clause is still not discharged: no characterization was run against `https://ymzhvcxod0.execute-api.us-east-1.amazonaws.com/prod/`. Three further findings, all pre-existing and none introduced here: (a) `src/backend/Makefile` states ten lines below the target *"Never call `make deploy` (create+execute) from automation"* — P-28's rule — yet `db-redesign-checks.yml` did exactly that, bypassing the CreateChangeSet-only + human-reviewer-gated-execute path `deploy.yml` implements correctly; (b) the step was labelled *"CDK deploy (Lambda code only)"*, which is false — it is a full CloudFormation deploy, and that label is how this stayed invisible for three steps; (c) `deploy-staging.yml` declares `STACK_NAME=CareerVpCrudStaging` but calls the same hardcoded `make deploy`, so **it deploys `CareerVpCrudDev` too** — a separate pre-existing bug, left alone. **FIXED 2026-07-31 at human instruction:** a new `deploy-devx` Makefile target deploys `$(DEVX_STACK_NAME) ?= CareerVpCrudDevx`, and `db-redesign-checks.yml` now sets `ENVIRONMENT: devx`, `STACK_NAME: CareerVpCrudDevx` and calls it. Deliberately a **separate target** rather than parameterizing `deploy`, because `make deploy` is invoked by **seven** workflows and repointing it globally would silently change staging. Verified by `make -n`: `deploy-devx` → `CareerVpCrudDevx`, `deploy` → `CareerVpCrudDev` unchanged. **Consequence for the next session: on `db-redesign`, a sync IS a deploy to `CareerVpCrudDevx`.** The first such push carries 3.2-GREEN + 3.3-GREEN together; it needs the isolated-diff proof in hand BEFORE the push, not after. **P-28 is still bypassed on this branch** — the job creates and executes in one step with `--require-approval=never`. Repointing the stack did not fix that, and a human should decide whether the `db-redesign` path should adopt `deploy.yml`'s change-set + gated-execute shape. **4. Removing line 941 did NOT clear the IAM union, and was never supposed to — verified live rather than assumed:** **9** attached `AWS::IAM::Policy` `...DefaultPolicy...` resources still grant `dynamodb:Scan` (`...CrudServiceRoleArnDefaultPolicy6B13FABA`, the CvUpload / vpr / CvTailor / CoverLetter / InterviewPrep worker roles, FailureHandler, ArtifactCleanup, BillingReconcile), sourced from the **22** implicit `grant_*_data` calls in `api_construct.py` (live count re-verified at 22). That surface is **3.4's** per DP-2. RED's IAM assertion is scoped to the inline statement precisely so 3.3 could not be blocked by it; narrowing those grants here would have been a rule-5 stop. **5. Residue, enumerated with owners, re-verified live (not copied):** `grep -rn "\.scan(" careervp/ scripts/` returns exactly **three** sites, every one already owned — `dal/dynamo_dal_handler.py:800` → **3.5** (left untouched on purpose; it is the site that would have made this step feel substantial); `dal/subscription_repository.py:415` → retained by Wave-2 `2.1-GREEN` for `BillingReconcileLambda`; `scripts/cr_migration_backfill.py:261` → offline, allow-listed, deleted at **3.5**. Also: the 22 implicit grants / 9 attached policies → **3.4**; `entity-index`'s `knowledgeType` partition key → **D-M5 (3.4)** and **Q-07 (Wave 4)**; `company_research_store::_legacy_table_name`, DAL-internal key building and `_legacy_read_cover_letter_by_scan`'s inner fallback → **3.5**; auth/trial keying → **Wave 6**; F-04 → **Wave 4**; P-07b / I-05 / I-06 untouched (I-05's red test was **not** silenced as a side effect). **No assertion was loosened anywhere.** **6. STILL OPEN FOR A HUMAN, inherited and not resolved here (3.3-GREEN may not edit the spec):** (a) 3.3-RED's flag that the spec's E-1 table and allow-list row 2 name `_legacy_read_cover_letter_by_scan` as the scan site when the live scan is in `legacy_read_cover_letter` (`:782`, scan at `:800`) — the RED baseline uses the live name and a human should fix the spec text; (b) 3.3-RED's two-node-ids-one-test-name resolution of the prompt-internal conflict; (c) the deliberately-unwritten "Scan still present in the attached policy" companion assertion; (d) `handlers/artifact_cleanup_handler.py:188` calls the non-existent `jobs_repo.scan_by_status(...)`, so the scheduled orphan sweep silently reaps nothing every run — a real latent bug with **no owner asserted**; (e) 3.2-GREEN's §0.3/§9.2 adversarial review for the v3.0.0 amendment is still undischarged. **7. Nothing blocks 3.4 or 3.5 from starting.** | **`14d943f`** — the whole implementation change (`api_construct.py -1` line) plus this row; the row's hash field was then corrected by the immediately following docs-only commit on `db-redesign` (a self-referential hash cannot name itself, so `14d943f` is the commit of record for 3.3-GREEN) | 2026-07-30 |
| 3.4 | D-M1, D-M2, D-M3, D-M5, D-M6, D-Q | **PROMPTS FILLED IN 2026-07-31 — not started.** `wave-3-prompts.md` §3.4 now carries a full pre-flight, four decision points and three prompts (3.4-SPEC → 3.4a → 3.4b, plus 3.4c for D-M6), replacing the 28-line skeleton. In plain English: 3.4 is bigger and rougher than the skeleton implied — its spec is a 62-line draft whose evidence is out of date, three of its six jobs have no written pass/fail criterion at all, and one job cannot be done without destroying and rebuilding a database table. None of that is a reason to stop; it is the reason 3.4 gets a SPEC step of its own, like 3.3 did. **Eight findings, all docs-only, zero source/test/infra files touched:** **F-1** the spec's Evidence is stale in four places (god-class is **1144** LOC not 1,128; knowledge table is `:430-460` not `:337-361`; `user_id-index` is `:151`/`:323` not `:115-117,235-237`; branch coverage is not zero — it is **55.79%** core). **F-2** AC-DM3-1, AC-DM6-1 and AC-DQ-1 **do not exist** — three clauses have a RED test listed with no acceptance criterion, and rule 14 forbids RED from inventing one. **F-3** D-M2's dual-key write is worse than "dual-key": one `put_item` at `dynamo_dal_handler.py:535-552` writes **three** overlapping conventions (canonical `applicationId`/`artifactId`, legacy `pk`/`sk`, and both `artifactType` **and** `artifact_type`); same shape at `:452`. **F-4** D-M5 requires a **table REPLACEMENT** — partition key `userEmail` at `:435-441` — which collides head-on with `B-3-4`'s zero-stateful-replacement assertion. **F-5** D-M5 will **break D-H7's `_GSI_BASELINE`** set equality by design, and 3.4b is authorized to re-record that one constant as a recorded schema decision (its own docstring provides for exactly this) — nothing else in either D-H7 file. Narrow scope: D-M3 changes projections only, and the baseline captures `(IndexName, KeySchema)`, so **projections do not break it**. **F-6** D-M3 is **8** sites, all `ProjectionType.ALL`; a projection change is a GSI delete+create, so it is sequenced per index, never batched. **F-7** D-Q is quantified and its TTL half is a **live defect**: PITR is **8** tables at 7 days (2 already at 35), and the TTL attribute is declared inconsistently (`expiration` ×5, `ttl`, `expiresAt`) **and mismatched against the writing code** — artifacts and knowledge tables declare `expiration` while the code writes `'ttl'`, so **those items never expire** (silent unbounded growth vs NFR-DATA-3). Connection reuse is unimplemented (no `botocore.config.Config` in `careervp/`). **F-8** `jobs_repository.py:115` queries `entity_type-index`, which is declared in `infra/careervp/specs/dynamodb_spec.yaml:118` but **was never built in CDK** — `list_jobs()` has **zero callers**, so this is dead code, not a live failure, and it is exactly the drift D-M6 exists to surface. | **3.4-SPEC runs first and must settle four things before any test is written:** **DP-3** — do D-M3/D-M6/D-Q stay in 3.4 at all, given F-2's missing ACs (recommended: D-M3 + D-Q stay, D-M6 becomes its own 3.4c so its inventory is written against post-3.4 keys — it is a `hard_dep_of: D-H8` and a stale inventory poisons Wave 6); **DP-4** — how D-M5's replacement is authorized (recommended: re-verify the live devx item count, record it, take a written `B-3-4` exception under v2.7.0/O-3; **a non-zero count is a STOP-and-ask, not a delete**, because "disposable" is a human ruling); **DP-5** — D-M3 as one change or eight (recommended: eight, one per index per deploy — one batch is the "single replacing change" `B-3-4` forbids); **DP-6** — what "minimized" means per index, which genuinely depends on D-M6's inventory (recommended: one cheap read-only inventory pass first, one authoritative pass at the end, and enumerate as residue any index whose readers are ambiguous rather than guessing). All four are defined with options, resolving evidence and stopping conditions in `wave-3-prompts.md` §3.4 — **deferred deliberately, and whoever resolves one owes a plain-English explanation in their row.** **Three new bets promoted to `ISSUES.md` before 3.4-SPEC runs (rule 9): `B-3-9`** (D-M5 can avoid a stateful replacement — expected FALSE, fallback pre-decided), **`B-3-10`** (GSI projections are minimizable from reading code alone — at least two indexes already known unreadable that way), **`B-3-11`** (D-Q's four items are independent — **FALSE for TTL**, which is bound to D-M2's write dict, so the two sub-steps must not both edit it). **Eleven bets are now re-read at the GATE, not eight.** **Recommended restructure, not taken unilaterally:** split into 3.4-SPEC → 3.4a (D-M1/D-M2, pure Python, no infra lock) → 3.4b (D-M3/D-M5/D-Q, holds the `infra/` lock, contains the only stateful replacement in the wave) → 3.4c (D-M6, doc, runs last). A human may reject the split. **Two items assigned to D-M6 at this fill-in** rather than left ownerless: `artifact_cleanup_handler.py:188`'s non-existent `jobs_repo.scan_by_status` and F-8's `entity_type-index` — D-M6 **inventories** both and names the required access pattern; it does not implement the reaper. **The `infra/` lock is free** — 3.3-GREEN released it 2026-07-31. | — (docs only) | 2026-07-31 |
| 3.2-CLOSEOUT-A | D-H4, P-01 | **PARTIALLY DONE — DO NOT MARK COMPLETE. Deploy, fixtures, reviews, spec and merge-blocker all landed; the live-API suite run did NOT produce a passing suite, and the reason is a defect this step was not authorized to fix.** In plain English: devx is now running the new code and the v3.0.0 rule was proven working against it by hand, but the two test suites that were supposed to prove it still cannot log in at all — they ask for the wrong kind of login ticket — so they fail before reaching anything they were meant to check. **Done:** (1) **Both adversarial reviews committed.** Round 1 recovered verbatim from session transcript `8ef79473-…jsonl` record 272 (claude-opus-5, 2026-07-31T11:19:37Z, opening prompt "run …adversarial-review-handoff.md") → `specs/amendments/adversarial-review-round1-opus.txt` with a provenance header; nothing reconstructed. Round 2 → `adversarial-review-round2-fable.txt`. (2) **Both live-API fixtures reconciled** — `'application_id': job_id` added to the `/interview-prep/generate` body in `tests/integration/test_full_pipeline_integration.py` and `tests/e2e/test_e2e_happy_path_full_job_application.py`; `job_id` confirmed in scope at both call sites (`:75`, `:39`). `git show HEAD -- src/backend/` is **exactly two changed lines**; no assertion, status, or other test touched. (3) **Spec inventory updated** — both fixtures added with the `pytest.skip`-on-unset-`API_BASE` explanation; the "other seven … all seven are the same class" sentence corrected to **six** suite-found same-class plus B1's CV-tailoring fixture (found via the B1 amendment, AC-DH4-2 `vpr_id` class, not AC-P01-1 `application_id`). **The total of eight is unchanged**; round 1's C3 is not acted on, per round 2's refutation. (4) **Merge blocker cleared** — `check_scope_lock_integrity.py --base origin/main --head HEAD` went `FAIL` → **`OK`** once the tip carried `Scope-Lock-Approved-By:`. (5) **devx deployed** — `UPDATE_COMPLETE 2026-08-01T09:12:51Z`, 264 resources, `Total time: 1052.49s`, exit 0; interview-prep API + worker Lambdas `LastModified 2026-08-01T09:17Z`. Wave-3's undeployed-debt item is discharged for D-H4/P-01. (6) **Characterization baseline written** — `docs/evidence/wave3-32closeouta-devx-characterization-20260801T094608Z.md` + `.json` + the `.probe.py` that produced it. **The one result that matters: the v3.0.0 contract is CONFIRMED LIVE on devx.** `POST /interview-prep/generate` without application identity → **HTTP 400 `{"error":"application_id/job_id is required","status_code":400,"code":"MISSING_REQUIRED_FIELD"}`** — the exact envelope AC-P01-1 pins. With `application_id` present it passes the identity guard and reaches upstream resolution, which proves the fixture fix was both correct and necessary. **The live-API suite run, quoted:** `8 failed, 4 passed, 12 skipped` — every failure is `401 UNAUTHORIZED` at the first authenticated hop, none reached an artifact wire; the 4 passes are `test_vpr_async_polling.py`, which never authenticates; with `API_BASE` unset the same command reports `4 passed, 20 skipped`, which is exactly how the two broken fixtures stayed invisible. **Also corrected for the record:** round 1's "the backend change is not deployed" was wrong about the code — it had been live on `CareerVpCrudDev` since 2026-07-27 (`…-lambda-dev LastModified 2026-07-30T10:27Z`) — and right about devx. | **STOP — flagged for human review, nothing here fixed by this step.** **1. `F-DEVX-1`, first-order: a completed VPR is never registered as a canonical artifact, so cover-letter and interview-prep are unreachable through the public API.** VPR `416cdaed-…` reached `completed`, yet `careervp-artifacts-table-devx` holds **0** items under `applicationId=<application>` for type vpr (only the company_research row) and **0** under the VPR request id; the record lives only in `careervp-jobs-table-devx` keyed by the *request* id. `POST /vpr/generate` also echoes its own `request_id` into the response's `job_id` field. Both downstream generators therefore return `409 {"status":"upstream_required","missing":["vpr"]}` forever. Locations: the VPR completion/registration path (`handlers/vpr_worker_handler.py`) vs the upstream check (`logic/artifact_dependency_utils.py`). **Owner not asserted** — it is adjacent to D-H4 canonical-artifact identity, and this step is forbidden from touching the worker path. **Note the inconsistency:** cv-tailoring resolved the same completed VPR and succeeded; only cover-letter and interview-prep fail. **Consequence: `F1` did NOT reproduce** — the request never reached a 202, so no worker ran. That is a different failure from F1 and is recorded as such. **2. `F-DEVX-2`: the live-API suites cannot authenticate.** Both helpers send the Cognito `access_token` (`e2e_helpers.py:118`, `integration_helpers.py:152`); the API Gateway authorizer accepts only the `id_token` (controlled comparison: same user, access→401, id→200). Until this is fixed **these suites cannot verify anything against any live stack**, and P-01's `e2e` mode cannot be claimed from them. Not fixed here — outside "add `application_id`, add NOTHING else". **3. `F-DEVX-3`: three stale request shapes** — `/users/me/cv` needs `{cv_content,file_name}` (the handler injects `user_id` only for that shape); `/jobs` needs `{title,company_name,description}` with a **required and reachability-checked** `url`; `/company-research/fetch` needs `{job_id}` not `{domain}`. **4. `F-DEVX-4`: all four artifact status polls hit routes that do not exist** — the API exposes only `/{id}/status`; bare `GET /vpr/{id}` returns `403 DEFAULT_4XX`, which reads as an auth failure but is an unrouted method. **5. `F-DEVX-5`: `/gap-analysis/questions` sits on the 29 s API-GW cap** — 27 965 ms (200) and 29 222 ms (**504**) on consecutive runs; when it 504s the chain silently empties. **6. `F-DEVX-6`: CV parse 500s** when the extractor emits `WorkExperience.company=None` against a required `str`. **7. `F-DEVX-7`, blocks CI: `make deploy-devx` cannot deploy devx.** The target added in `a8ef789` omits `-c p26_rehome_features=true`, which scope-lock v2.6.0 requires and which is absent from `infra/cdk.json` (and `cdk` runs from `src/backend`, which has no `cdk.json`). Without it the 76 P-26 resources synthesize into the **parent** stack with new logical ids and CFN aborts: *"Resource of type 'AWS::Logs::LogGroup' … already exists"* ×27 plus the CodeDeploy application. Synth proof: without the flag 32 parent / 0 nested log groups; with it 2 parent / 30 nested, logical ids matching the deployed stack. **So `db-redesign-checks.yml`'s `deploy-backend-dev` will fail on every push touching `src/backend/**` or `infra/**` — the a8ef789 repoint is not yet functional.** This step deployed by invoking the documented v2.6.0 command directly with the flag and with `allowed_origins` read from `infra/cdk.json` (CORS unchanged); the Makefile was deliberately **not** edited — `infra/` belongs to 3.4. **8. Residual spec inconsistency NOT fixed, deliberately:** the inventory's closing arithmetic line "plus the one the proposal did name (1) and its sibling above = 8 total" double-counts `marks_failed`; round 2 flagged this. The prompt authorized correcting **one** sentence and forbade changing the total, so the line is left as-is for a human. **9. Unchanged and still open from 3.2-GREEN:** the §0.3/§9.2 adversarial review for v3.0.0 is now *documented* (both rounds committed) but a human still owes its disposition; P-28 defect N2 (the integrity checker reads only the tip commit) is untouched and **the trailer mitigation on this step's commits decays the moment a later commit moves the tip** — the real fix is to iterate the commit range and is owned by **P-28**, not this step. | `ea5eb74` (reviews + fixtures + spec) · evidence + this row land in the immediately following commit on `db-redesign` (a self-referential hash cannot name itself; `ea5eb74` is the commit of record for the code change) | 2026-08-01 |
| 3.2-CLOSEOUT-B | D-H4, P-01, **D-H10 (new)** | **PROMPT WRITTEN 2026-08-01 — not started.** Prepares, but does not apply, one twin-synced scope-lock edit carrying both the v3.0.0 record corrections and the new **D-H10** clause. Agent drafts; **a human commits** — §0.3 write-protection, and round-1 defect N1 found that protection already defeated twice with the CI guard unable to tell. Target version **4.0.0** (MAJOR: §0.3's ladder counts "drop a feature", and `VPRGenerateRequest.force` is a public request field being removed). | Human owes the v4.0.0 twin commit with a `Scope-Lock-Approved-By:` trailer. **3.6 may not start until it lands.** Agent must state its own reading on whether §9.2 requires an adversarial review for D-H10 — the requirement keys on IMMUTABLE invariants, locked decisions, or the ten §3 items, and none of the ten is changed. | — | — |
| 3.6-SPEC | **D-H10 (new)** | **PROMPT WRITTEN 2026-08-01 — not started. Runs BEFORE 3.4** (see `wave-3-prompts.md` §2 order note). Authors `specs/D-H10-artifact-editing-model-spec.md`, which does not exist. Human decision of 2026-08-01: artifacts are generated once and thereafter edited in place via the existing AI Assist inline editor; **Retry on a FAILED artifact stays, Regenerate on a COMPLETED one goes**, and `force` is rejected rather than ignored. Note `POST /ai/assist` (FE-UI-046/047) ships in production today with **no clause in either twin** — D-H10 is the first clause to govern it. | **Two deferred decision points, both owned here. DP-1 can block the whole step:** AI Assist rewrites ONE FIELD; regeneration replaced a whole artifact. Inventory live whether every regenerable artifact type has a reachable inline edit path — if any does not, **STOP** and emit a rule-5 flag rather than narrowing D-H10, because removing the only remedy for a bad artifact is a product regression the human has not agreed to. **DP-2:** does `APIModel` forbid extra fields? That decides whether rejecting `force` is free via Pydantic or needs an explicit guard — and if a guard, its body uses the §3 item-10 shape, **not** the `{error, status_code, code}` family both reviews found non-conformant. **New bets `B-3-9` and `B-3-10` must be seeded in `ISSUES.md` before this runs.** | — | — |
| 3.6-RED | **D-H10 (new)** | **PROMPT WRITTEN 2026-08-01 — not started.** Writes the tests only, in three labelled groups. Group A (AI Assist sets `edited`, preserves `artifact_id`, persists via the §3 item-5 PATCH contract; a completed artifact yields one stable id for life; Retry survives) is **expected to pass immediately** — it proves the replacement works. Groups B and C (force rejected, no Regenerate control, regeneration symbols absent) are expected to fail. | **A failing Group A test is a first-order finding, not a normal RED.** It means the replacement for regeneration does not function and D-H10 cannot proceed — STOP and flag it. | — | — |
| 3.6-GREEN | **D-H10 (new)** | **PROMPT WRITTEN 2026-08-01 — not started.** Removes regeneration across backend, frontend, and tests. Fable per rule 18 (multi-file, cross-boundary, against a pinned spec). Frontend is **in scope by name** — the Regenerate control and its confirmation dialog — as a named cross-boundary deliverable, not a silent expansion. Negative proof (instrument, observe zero, record the count) required before the `force_regenerate` branch is deleted. | **Leave F1 alone** — the interview-prep worker's surviving `vpr_id` fallback at `interview_prep_handler.py:848-860` becomes unreachable residue once regeneration is gone, and **3.5 owns deleting it** as a D-H9 retirement-register item. Verify against the characterization baseline from 3.2-CLOSEOUT-A and call out any behavior change not explained by the removal. | — | — |
| 3.4 | — | *(see the 3.4 row above — unchanged, but now **runs after 3.6**, so that D-M6's access-pattern document describes the post-removal surface rather than a path about to be deleted)* | — | — | — |
| 3.5 | D-H9 | not started (skeleton — fill in **after the v4.0.0 twin commit and 3.6-GREEN**, not merely after 3.1-GREEN; the demolition list gains new items from the D-H10 removal and from round-2 findings F1/F2) | — | — | — |
| GATE | — | not started | — | — | — |

---

## Wave-3 bets (rule 9 — ✅ PROMOTED to `ISSUES.md` 2026-07-27)

Rule 9 requires every belief a wave rests on to be written down *before* its prompts run, with the
cheapest check that would disprove it and the fallback decided now. **All eight live in
`/Users/yitzchak.meirovich/Documents/code5/careervp/docs/db-redesign/code/code-analysis/project/ISSUES.md`
under "Wave-3 bets"** — that is the authoritative text (belief, why it is a bet, cheapest-tier check,
fallback decided now, settled-status). The table below is the index; read the full entries there, and
re-read every one at the GATE.

`B-3-5` is new — added during the 2026-07-27 pre-flight when the two D-H2 static scans turned out to
have no scope boundary at all. It is the bet whose absence blocked Wave-2 step 2.2.
`B-3-6` and `B-3-7` were added by 3.2-SPEC before 3.2-RED, following that precedent.
`B-3-8` was added at the 2026-07-29 3.3 fill-in, before 3.3-SPEC ran, when the pre-flight scan
inventory found the D-H7 spec's Evidence pointing at sites other steps had already decided.
**Eight bets are re-read at the GATE, not seven.**

| Bet | Belief (stated so it could be false) | Cheapest check (rule 9 ladder) | Fallback if false |
|---|---|---|---|
| B-3-1 | The migration-parity harness (built in 3.1) can assert *exact* public-projection equality between a legacy read and the canonical read for every migrated slice, with no benign diffs. **SETTLED FALSE 2026-07-27 — fallback taken.** | Tier 3 — one minimal moto test: seed one legacy + one canonical record for a single artifact type, run the harness, inspect the diff. **Not needed: settled by Tier 1 instead.** A live read of the DAL write paths found benign diffs are guaranteed, not hypothetical — the cover-letter write at `dynamo_dal_handler.py:535-552` puts *both* key conventions and *both* spellings of the type field on one item, and the artifacts/gap-responses/knowledge tables declare `time_to_live_attribute="expiration"` while the DAL writes `ttl`. Exact raw equality was never achievable. | **Fallback taken, in force.** The harness normalizes against a documented internal-field allowlist, now **enumerated with its derivation rule** in `D-H2-D-H3-key-authority-spec.md` (10 attributes, each with live evidence). Grows only by a dated ledger entry. **RETIRED at scope-lock v2.7.0** — the harness no longer exists; nothing in Wave 3 depends on this bet. Full entry kept in `ISSUES.md` for the GATE re-read. |
| B-3-2 | The swallowed `ValidationException`s D-H3 targets are actually reachable on the request path, not dead defensive `except` blocks. | Tier 1 — grep the DAL/handler `except` sites that convert `ValidationException`→`None`/404; Tier 3 — a moto test forcing a malformed key. | If unreachable, D-H3 ships as a guard-rail + regression test (surface-and-log), not a behavior change; record that it changed nothing live and why. |
| B-3-3 | The "239 legacy CR items" figure D-H9 (3.5) backfills is still accurate at Wave-3 time. **RETIRED at scope-lock v2.7.0 — there is no backfill.** | Tier 1 — live count in **devx** before 3.5. **Not needed:** D-H9 was repointed to demolition; legacy CR items are deleted, not migrated. | None required. The demolition gate asks a different question — not "how many items are there" but "does anything still read them". |
| B-3-4 | Wave-3's GSI changes (3.4 minimized GSI, retire `userEmail` PK) stay under the CFN resource ceiling and cause zero stateful replacement. Carries `B-2-3` forward. | Tier 4 — **isolated synth-template diff** per infra-touching step (HEAD vs change-stashed, no live stack — the 2.3-root-cause technique); assert zero replacement markers on stateful resources. | Sequence the GSI add/remove as separate gated deploys (add new → dual-read → drop old), never a single replacing change. |
| B-3-5 | D-H2's two static scans can be scoped to the **artifacts/core** table without leaving a hole — user/application/jobs keying stays for Wave-6 D-H8. | Tier 1 — enumerate every candidate site live and classify it, **before** the test is written. Done 2026-07-27. | Ship the scan as a **frozen-baseline ratchet** (enumerated sites, may shrink never grow) rather than an absolute zero-occurrences assertion the wave is not scoped to satisfy. |
| B-3-6 | AC-DH4-2 still fails against the live backend. **SETTLED TRUE 2026-07-27 at the handler boundary:** the model rejects omission, but the handler bypasses the model and accepts omission exactly like present-null. | Tier 1 — read `CVTailoringRequest` and `_validate_openapi_cv_tailoring_payload`; a zero-file response probe corroborated both payloads returning the same stubbed 202. | If false, keep a labelled day-one regression guard. Not taken: the handler assertion is behavior-changing RED. Keep F-04 open and flag its Wave-4 ownership overlap. |
| B-3-7 | AC-DH4-1's hub round-trip is broken on the read side while the write path is sound. **SETTLED FALSE 2026-07-27:** the write exception is swallowed and the hub serves a null id over an existing artifact. | Tier 3 — one minimal moto probe: seed application + canonical artifact, force step-2 `update_item` failure, then read the real hub projection. | Fallback taken: pin `application_repository.py`; surface `Result(... code=DYNAMODB_ERROR)` and require the canonical hub read to resolve the stored id. D-H4 owns it; D-H3 is unchanged. |
| B-3-9 | D-M5's `userEmail` PK retirement can be done **without a stateful table replacement**. **Added 2026-07-31 at the 3.4 fill-in; expected FALSE** — a DynamoDB partition-key change is a CFN delete+create, and `B-3-4` asserts zero stateful replacement on every infra step. | Tier 1 — read `api_db_construct.py:435-441`, then one isolated synth-template diff looking for a replacement marker. | Take DP-4 Option A: re-verify the live devx item count, record it, and take the replacement as an explicitly recorded `B-3-4` exception under v2.7.0/O-3. **A non-zero count is a STOP-and-ask, not a delete.** If the human declines, D-M5 defers to Q-07 (Wave 4). |
| B-3-10 | D-M3's GSI projections can be minimized **from the reading code alone**, without waiting on D-M6's inventory. | Tier 1 — enumerate live callers per index name across `careervp/`. | DP-6 Option A: D-M6 runs a cheap read-only inventory pass FIRST; D-M3 minimizes only unambiguous indexes and **enumerates the rest as residue** with an owner. Zero callers is evidence the index may be dead, not evidence for `KEYS_ONLY`. |
| B-3-11 | D-Q's four quick wins are **independent** of each other and of D-M2. | Tier 1 — compare each table's `time_to_live_attribute` against the attribute the writing code actually puts. | **FALSE for TTL** (verified at fill-in): it shares `dynamo_dal_handler.py`'s write dict with D-M2, so it ships in whichever sub-step owns that dict — decided once at 3.4-SPEC, never by both. PITR (8 sites) and connection reuse are independent and may ship standalone in 3.4b; pagination must be quantified live or moved out with a named owner. |
| B-3-8 | There is a live request-path Scan that is **3.3's own** to remove. **Added 2026-07-29 at 3.3 fill-in; PRE-SETTLED TOWARD FALSE** — all three live `.scan(` sites are already owned by other decisions (reconcile scan kept by Wave-2 2.1-GREEN; legacy cover-letter scan owned by 3.5; backfill script allow-listed). | Tier 1 — `grep -rn "\.scan(" careervp/ scripts/` and classify each hit against existing decisions, not against the spec. 3.3-SPEC settles it as its first action. | D-H7/3.3 ships as **guard-rail + regression test only** (static source guard + GSI-shape assertion), no behavior change — `B-3-2`'s shape. Guards are labelled as guards, the row records they passed day one, and 3.3 neither hunts for a scan to justify itself nor annexes `dynamo_dal_handler.py:800` from 3.5. |

---

## ✅ SCOPE-LOCK AMENDMENT LANDED — v2.7.0, 2026-07-27

**Decision:** all stored data in every environment is disposable test data, dropped before
production. Wave 3+ is **forward-thinking only** — no migration, no dual-read, no backfill, no
cutover. A record in a legacy shape is deleted and rewritten, never migrated.

**Landed in `project-scope-lock.{md,yaml}` at v2.7.0** (proposal:
`../specs/amendments/D-H2-harness-removal-amendment.md`):

- **Harness retired** from D-H2; `migration-parity` dropped from D-H4 / D-M2 / D-M5 / D-H9
  verification. A14 homed it to prove a live-data cutover; there is no cutover.
- **D-H9 repointed** from "complete the FE-UI-044 CR migration" to **legacy-path demolition gated
  by a retirement register**. The retirement half of the clause is kept verbatim; the migration
  half is dropped. `../specs/D-H9-legacy-path-demolition-spec.md` is the spec of record and
  supersedes `D-H9-company-research-migration-spec.md`, which is deleted.
- **O-3 resolved** (cutover/downtime tolerance + retention window). It was OPEN and formally
  `blocks: [wave_3, wave_6]` — a question that gated this entire wave.
- **Bets `B-3-1` and `B-3-3` retired** in `ISSUES.md`, kept rather than deleted so rule 9's
  re-read at the GATE does not mistake a vanished bet for an unsettled one.

**Also landed alongside, not part of the contract change:** `RUNBOOK-RULES.md` **rule 18** (Fable
routed to long-horizon implementation only — never RED, recon, GATE, or security work), a fourth
bucket in the execution plan's model convention, and the D-M6/D-Q clause-mapping fix (the seams
bundle dropped both clauses; `D-M6-D-Q-canonical-storage-shape-spec.md` now owns them outright, so
`scope-diff.py`'s clause→spec mapping is deterministic again).

**Caveat recorded, not hidden:** §0.3 says the contract twins are write-protected from agent
sessions and land only via a human-executed commit. This amendment was **applied by an agent
session at explicit human direction**, with the approval trailer naming the human approver. If the
governance intent is that a human literally authors the twin diff, amend the commit.

---

## Standing notes carried into every step (do not lose these)

- **The two IMMUTABLE laws apply to every row that touches `infra/`:** never move the live API
  (`RestApi`), never move the Cognito user pool. Internal PK/SK/table changes are fine; the API
  Gateway logical id and the Cognito pool logical id stay byte-stable.
- **Frontend §3 identifiers and response shapes may not drift.** D-H2/D-H4/D-M all change *internal*
  keying; none of that is an API change. The executable oracle (Wave-0 step 0.3,
  `/Users/yitzchak.meirovich/Documents/code5/careervp/docs/db-redesign/code/code-analysis/project/specs/F-frontend-oracle-spec.md`)
  is the contract check — a Wave-3 change that alters a §3 identifier is a rule-5 stop.
- **Migration-parity discipline (v2.0.0/A14) is RETIRED — superseded by v2.7.0 / `O-3`** (see the
  amendment section above). Wave 3 is forward-thinking only: no migration, no dual-read window, no
  backfill, no cutover, and **no parity harness** — 3.1 does not build one and 3.2/3.4/3.5 do not
  consume one. A record in a legacy shape is deleted and rewritten. D-H2's `verification` is `unit`.
- **`CoreRepository` / `TableRegistry` (created by 3.1) is the Wave-3 contention hotspot** — the
  analogue of `api_construct.py` in Wave 2. Every later step extends it. Never run two steps that
  edit it at the same time; see `wave-3-prompts.md` §2 for the serialization order.
- **Deploy target is `CareerVpCrudDevx`; deploys are manual-dispatch only; nothing merges to
  `main`** (Wave-2 `B-2-4` item 1 open). Anything at `api.dev.careervp.com` is the OLD stack.
- Carried in from earlier waves, still open, none of which gate Wave 3: **P-07b** (browser admin
  scope + implicit grant — gates STAGING, has a written stopping condition); **I-05** (AI-assist
  token-metering red test — belongs to the metering clause, do not silence it inside a Wave-3 step);
  **I-06** (login client admin scope). Do not fix these as a side effect of a Wave-3 step.
